// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
	"gitlab.jiguang.dev/pos-dine/dine/ent/storeaccount"
)

// StoreAccount is the model entity for the StoreAccount schema.
type StoreAccount struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 关联门店
	StoreID int `json:"store_id,omitempty"`
	// 账户余额（可提现金额）
	Balance decimal.Decimal `json:"balance,omitempty"`
	// 待提现金额
	PendingWithdraw decimal.Decimal `json:"pending_withdraw,omitempty"`
	// 已提现金额
	Withdrawn decimal.Decimal `json:"withdrawn,omitempty"`
	// 总收益金额
	TotalAmount decimal.Decimal `json:"total_amount,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StoreAccountQuery when eager-loading is set.
	Edges        StoreAccountEdges `json:"edges"`
	selectValues sql.SelectValues
}

// StoreAccountEdges holds the relations/edges for other nodes in the graph.
type StoreAccountEdges struct {
	// Store holds the value of the store edge.
	Store *Store `json:"store,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// StoreOrErr returns the Store value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StoreAccountEdges) StoreOrErr() (*Store, error) {
	if e.Store != nil {
		return e.Store, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: store.Label}
	}
	return nil, &NotLoadedError{edge: "store"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StoreAccount) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case storeaccount.FieldBalance, storeaccount.FieldPendingWithdraw, storeaccount.FieldWithdrawn, storeaccount.FieldTotalAmount:
			values[i] = new(decimal.Decimal)
		case storeaccount.FieldID, storeaccount.FieldDeletedAt, storeaccount.FieldStoreID:
			values[i] = new(sql.NullInt64)
		case storeaccount.FieldCreatedAt, storeaccount.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StoreAccount fields.
func (sa *StoreAccount) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case storeaccount.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sa.ID = int(value.Int64)
		case storeaccount.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sa.CreatedAt = value.Time
			}
		case storeaccount.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sa.UpdatedAt = value.Time
			}
		case storeaccount.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				sa.DeletedAt = value.Int64
			}
		case storeaccount.FieldStoreID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value.Valid {
				sa.StoreID = int(value.Int64)
			}
		case storeaccount.FieldBalance:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field balance", values[i])
			} else if value != nil {
				sa.Balance = *value
			}
		case storeaccount.FieldPendingWithdraw:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field pending_withdraw", values[i])
			} else if value != nil {
				sa.PendingWithdraw = *value
			}
		case storeaccount.FieldWithdrawn:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field withdrawn", values[i])
			} else if value != nil {
				sa.Withdrawn = *value
			}
		case storeaccount.FieldTotalAmount:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field total_amount", values[i])
			} else if value != nil {
				sa.TotalAmount = *value
			}
		default:
			sa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StoreAccount.
// This includes values selected through modifiers, order, etc.
func (sa *StoreAccount) Value(name string) (ent.Value, error) {
	return sa.selectValues.Get(name)
}

// QueryStore queries the "store" edge of the StoreAccount entity.
func (sa *StoreAccount) QueryStore() *StoreQuery {
	return NewStoreAccountClient(sa.config).QueryStore(sa)
}

// Update returns a builder for updating this StoreAccount.
// Note that you need to call StoreAccount.Unwrap() before calling this method if this StoreAccount
// was returned from a transaction, and the transaction was committed or rolled back.
func (sa *StoreAccount) Update() *StoreAccountUpdateOne {
	return NewStoreAccountClient(sa.config).UpdateOne(sa)
}

// Unwrap unwraps the StoreAccount entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sa *StoreAccount) Unwrap() *StoreAccount {
	_tx, ok := sa.config.driver.(*txDriver)
	if !ok {
		panic("ent: StoreAccount is not a transactional entity")
	}
	sa.config.driver = _tx.drv
	return sa
}

// String implements the fmt.Stringer.
func (sa *StoreAccount) String() string {
	var builder strings.Builder
	builder.WriteString("StoreAccount(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sa.ID))
	builder.WriteString("created_at=")
	builder.WriteString(sa.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sa.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", sa.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", sa.StoreID))
	builder.WriteString(", ")
	builder.WriteString("balance=")
	builder.WriteString(fmt.Sprintf("%v", sa.Balance))
	builder.WriteString(", ")
	builder.WriteString("pending_withdraw=")
	builder.WriteString(fmt.Sprintf("%v", sa.PendingWithdraw))
	builder.WriteString(", ")
	builder.WriteString("withdrawn=")
	builder.WriteString(fmt.Sprintf("%v", sa.Withdrawn))
	builder.WriteString(", ")
	builder.WriteString("total_amount=")
	builder.WriteString(fmt.Sprintf("%v", sa.TotalAmount))
	builder.WriteByte(')')
	return builder.String()
}

// StoreAccounts is a parsable slice of StoreAccount.
type StoreAccounts []*StoreAccount
