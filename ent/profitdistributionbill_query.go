// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchant"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/profitdistributionbill"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
)

// ProfitDistributionBillQuery is the builder for querying ProfitDistributionBill entities.
type ProfitDistributionBillQuery struct {
	config
	ctx          *QueryContext
	order        []profitdistributionbill.OrderOption
	inters       []Interceptor
	predicates   []predicate.ProfitDistributionBill
	withMerchant *MerchantQuery
	withStore    *StoreQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProfitDistributionBillQuery builder.
func (pdbq *ProfitDistributionBillQuery) Where(ps ...predicate.ProfitDistributionBill) *ProfitDistributionBillQuery {
	pdbq.predicates = append(pdbq.predicates, ps...)
	return pdbq
}

// Limit the number of records to be returned by this query.
func (pdbq *ProfitDistributionBillQuery) Limit(limit int) *ProfitDistributionBillQuery {
	pdbq.ctx.Limit = &limit
	return pdbq
}

// Offset to start from.
func (pdbq *ProfitDistributionBillQuery) Offset(offset int) *ProfitDistributionBillQuery {
	pdbq.ctx.Offset = &offset
	return pdbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pdbq *ProfitDistributionBillQuery) Unique(unique bool) *ProfitDistributionBillQuery {
	pdbq.ctx.Unique = &unique
	return pdbq
}

// Order specifies how the records should be ordered.
func (pdbq *ProfitDistributionBillQuery) Order(o ...profitdistributionbill.OrderOption) *ProfitDistributionBillQuery {
	pdbq.order = append(pdbq.order, o...)
	return pdbq
}

// QueryMerchant chains the current query on the "merchant" edge.
func (pdbq *ProfitDistributionBillQuery) QueryMerchant() *MerchantQuery {
	query := (&MerchantClient{config: pdbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(profitdistributionbill.Table, profitdistributionbill.FieldID, selector),
			sqlgraph.To(merchant.Table, merchant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, profitdistributionbill.MerchantTable, profitdistributionbill.MerchantColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStore chains the current query on the "store" edge.
func (pdbq *ProfitDistributionBillQuery) QueryStore() *StoreQuery {
	query := (&StoreClient{config: pdbq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdbq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdbq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(profitdistributionbill.Table, profitdistributionbill.FieldID, selector),
			sqlgraph.To(store.Table, store.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, profitdistributionbill.StoreTable, profitdistributionbill.StoreColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdbq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProfitDistributionBill entity from the query.
// Returns a *NotFoundError when no ProfitDistributionBill was found.
func (pdbq *ProfitDistributionBillQuery) First(ctx context.Context) (*ProfitDistributionBill, error) {
	nodes, err := pdbq.Limit(1).All(setContextOp(ctx, pdbq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{profitdistributionbill.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) FirstX(ctx context.Context) *ProfitDistributionBill {
	node, err := pdbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProfitDistributionBill ID from the query.
// Returns a *NotFoundError when no ProfitDistributionBill ID was found.
func (pdbq *ProfitDistributionBillQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pdbq.Limit(1).IDs(setContextOp(ctx, pdbq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{profitdistributionbill.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pdbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProfitDistributionBill entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProfitDistributionBill entity is found.
// Returns a *NotFoundError when no ProfitDistributionBill entities are found.
func (pdbq *ProfitDistributionBillQuery) Only(ctx context.Context) (*ProfitDistributionBill, error) {
	nodes, err := pdbq.Limit(2).All(setContextOp(ctx, pdbq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{profitdistributionbill.Label}
	default:
		return nil, &NotSingularError{profitdistributionbill.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) OnlyX(ctx context.Context) *ProfitDistributionBill {
	node, err := pdbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProfitDistributionBill ID in the query.
// Returns a *NotSingularError when more than one ProfitDistributionBill ID is found.
// Returns a *NotFoundError when no entities are found.
func (pdbq *ProfitDistributionBillQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pdbq.Limit(2).IDs(setContextOp(ctx, pdbq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{profitdistributionbill.Label}
	default:
		err = &NotSingularError{profitdistributionbill.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pdbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProfitDistributionBills.
func (pdbq *ProfitDistributionBillQuery) All(ctx context.Context) ([]*ProfitDistributionBill, error) {
	ctx = setContextOp(ctx, pdbq.ctx, ent.OpQueryAll)
	if err := pdbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProfitDistributionBill, *ProfitDistributionBillQuery]()
	return withInterceptors[[]*ProfitDistributionBill](ctx, pdbq, qr, pdbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) AllX(ctx context.Context) []*ProfitDistributionBill {
	nodes, err := pdbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProfitDistributionBill IDs.
func (pdbq *ProfitDistributionBillQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pdbq.ctx.Unique == nil && pdbq.path != nil {
		pdbq.Unique(true)
	}
	ctx = setContextOp(ctx, pdbq.ctx, ent.OpQueryIDs)
	if err = pdbq.Select(profitdistributionbill.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pdbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pdbq *ProfitDistributionBillQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pdbq.ctx, ent.OpQueryCount)
	if err := pdbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pdbq, querierCount[*ProfitDistributionBillQuery](), pdbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) CountX(ctx context.Context) int {
	count, err := pdbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pdbq *ProfitDistributionBillQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pdbq.ctx, ent.OpQueryExist)
	switch _, err := pdbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pdbq *ProfitDistributionBillQuery) ExistX(ctx context.Context) bool {
	exist, err := pdbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProfitDistributionBillQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pdbq *ProfitDistributionBillQuery) Clone() *ProfitDistributionBillQuery {
	if pdbq == nil {
		return nil
	}
	return &ProfitDistributionBillQuery{
		config:       pdbq.config,
		ctx:          pdbq.ctx.Clone(),
		order:        append([]profitdistributionbill.OrderOption{}, pdbq.order...),
		inters:       append([]Interceptor{}, pdbq.inters...),
		predicates:   append([]predicate.ProfitDistributionBill{}, pdbq.predicates...),
		withMerchant: pdbq.withMerchant.Clone(),
		withStore:    pdbq.withStore.Clone(),
		// clone intermediate query.
		sql:       pdbq.sql.Clone(),
		path:      pdbq.path,
		modifiers: append([]func(*sql.Selector){}, pdbq.modifiers...),
	}
}

// WithMerchant tells the query-builder to eager-load the nodes that are connected to
// the "merchant" edge. The optional arguments are used to configure the query builder of the edge.
func (pdbq *ProfitDistributionBillQuery) WithMerchant(opts ...func(*MerchantQuery)) *ProfitDistributionBillQuery {
	query := (&MerchantClient{config: pdbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pdbq.withMerchant = query
	return pdbq
}

// WithStore tells the query-builder to eager-load the nodes that are connected to
// the "store" edge. The optional arguments are used to configure the query builder of the edge.
func (pdbq *ProfitDistributionBillQuery) WithStore(opts ...func(*StoreQuery)) *ProfitDistributionBillQuery {
	query := (&StoreClient{config: pdbq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pdbq.withStore = query
	return pdbq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProfitDistributionBill.Query().
//		GroupBy(profitdistributionbill.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pdbq *ProfitDistributionBillQuery) GroupBy(field string, fields ...string) *ProfitDistributionBillGroupBy {
	pdbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProfitDistributionBillGroupBy{build: pdbq}
	grbuild.flds = &pdbq.ctx.Fields
	grbuild.label = profitdistributionbill.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProfitDistributionBill.Query().
//		Select(profitdistributionbill.FieldCreatedAt).
//		Scan(ctx, &v)
func (pdbq *ProfitDistributionBillQuery) Select(fields ...string) *ProfitDistributionBillSelect {
	pdbq.ctx.Fields = append(pdbq.ctx.Fields, fields...)
	sbuild := &ProfitDistributionBillSelect{ProfitDistributionBillQuery: pdbq}
	sbuild.label = profitdistributionbill.Label
	sbuild.flds, sbuild.scan = &pdbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProfitDistributionBillSelect configured with the given aggregations.
func (pdbq *ProfitDistributionBillQuery) Aggregate(fns ...AggregateFunc) *ProfitDistributionBillSelect {
	return pdbq.Select().Aggregate(fns...)
}

func (pdbq *ProfitDistributionBillQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pdbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pdbq); err != nil {
				return err
			}
		}
	}
	for _, f := range pdbq.ctx.Fields {
		if !profitdistributionbill.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pdbq.path != nil {
		prev, err := pdbq.path(ctx)
		if err != nil {
			return err
		}
		pdbq.sql = prev
	}
	return nil
}

func (pdbq *ProfitDistributionBillQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProfitDistributionBill, error) {
	var (
		nodes       = []*ProfitDistributionBill{}
		_spec       = pdbq.querySpec()
		loadedTypes = [2]bool{
			pdbq.withMerchant != nil,
			pdbq.withStore != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProfitDistributionBill).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProfitDistributionBill{config: pdbq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(pdbq.modifiers) > 0 {
		_spec.Modifiers = pdbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pdbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pdbq.withMerchant; query != nil {
		if err := pdbq.loadMerchant(ctx, query, nodes, nil,
			func(n *ProfitDistributionBill, e *Merchant) { n.Edges.Merchant = e }); err != nil {
			return nil, err
		}
	}
	if query := pdbq.withStore; query != nil {
		if err := pdbq.loadStore(ctx, query, nodes, nil,
			func(n *ProfitDistributionBill, e *Store) { n.Edges.Store = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pdbq *ProfitDistributionBillQuery) loadMerchant(ctx context.Context, query *MerchantQuery, nodes []*ProfitDistributionBill, init func(*ProfitDistributionBill), assign func(*ProfitDistributionBill, *Merchant)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProfitDistributionBill)
	for i := range nodes {
		fk := nodes[i].MerchantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(merchant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "merchant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pdbq *ProfitDistributionBillQuery) loadStore(ctx context.Context, query *StoreQuery, nodes []*ProfitDistributionBill, init func(*ProfitDistributionBill), assign func(*ProfitDistributionBill, *Store)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProfitDistributionBill)
	for i := range nodes {
		fk := nodes[i].StoreID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(store.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "store_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pdbq *ProfitDistributionBillQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pdbq.querySpec()
	if len(pdbq.modifiers) > 0 {
		_spec.Modifiers = pdbq.modifiers
	}
	_spec.Node.Columns = pdbq.ctx.Fields
	if len(pdbq.ctx.Fields) > 0 {
		_spec.Unique = pdbq.ctx.Unique != nil && *pdbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pdbq.driver, _spec)
}

func (pdbq *ProfitDistributionBillQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(profitdistributionbill.Table, profitdistributionbill.Columns, sqlgraph.NewFieldSpec(profitdistributionbill.FieldID, field.TypeUUID))
	_spec.From = pdbq.sql
	if unique := pdbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pdbq.path != nil {
		_spec.Unique = true
	}
	if fields := pdbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, profitdistributionbill.FieldID)
		for i := range fields {
			if fields[i] != profitdistributionbill.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pdbq.withMerchant != nil {
			_spec.Node.AddColumnOnce(profitdistributionbill.FieldMerchantID)
		}
		if pdbq.withStore != nil {
			_spec.Node.AddColumnOnce(profitdistributionbill.FieldStoreID)
		}
	}
	if ps := pdbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pdbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pdbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pdbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pdbq *ProfitDistributionBillQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pdbq.driver.Dialect())
	t1 := builder.Table(profitdistributionbill.Table)
	columns := pdbq.ctx.Fields
	if len(columns) == 0 {
		columns = profitdistributionbill.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pdbq.sql != nil {
		selector = pdbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pdbq.ctx.Unique != nil && *pdbq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pdbq.modifiers {
		m(selector)
	}
	for _, p := range pdbq.predicates {
		p(selector)
	}
	for _, p := range pdbq.order {
		p(selector)
	}
	if offset := pdbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pdbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (pdbq *ProfitDistributionBillQuery) ForUpdate(opts ...sql.LockOption) *ProfitDistributionBillQuery {
	if pdbq.driver.Dialect() == dialect.Postgres {
		pdbq.Unique(false)
	}
	pdbq.modifiers = append(pdbq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return pdbq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (pdbq *ProfitDistributionBillQuery) ForShare(opts ...sql.LockOption) *ProfitDistributionBillQuery {
	if pdbq.driver.Dialect() == dialect.Postgres {
		pdbq.Unique(false)
	}
	pdbq.modifiers = append(pdbq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return pdbq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pdbq *ProfitDistributionBillQuery) Modify(modifiers ...func(s *sql.Selector)) *ProfitDistributionBillSelect {
	pdbq.modifiers = append(pdbq.modifiers, modifiers...)
	return pdbq.Select()
}

// ProfitDistributionBillGroupBy is the group-by builder for ProfitDistributionBill entities.
type ProfitDistributionBillGroupBy struct {
	selector
	build *ProfitDistributionBillQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pdbgb *ProfitDistributionBillGroupBy) Aggregate(fns ...AggregateFunc) *ProfitDistributionBillGroupBy {
	pdbgb.fns = append(pdbgb.fns, fns...)
	return pdbgb
}

// Scan applies the selector query and scans the result into the given value.
func (pdbgb *ProfitDistributionBillGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pdbgb.build.ctx, ent.OpQueryGroupBy)
	if err := pdbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProfitDistributionBillQuery, *ProfitDistributionBillGroupBy](ctx, pdbgb.build, pdbgb, pdbgb.build.inters, v)
}

func (pdbgb *ProfitDistributionBillGroupBy) sqlScan(ctx context.Context, root *ProfitDistributionBillQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pdbgb.fns))
	for _, fn := range pdbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pdbgb.flds)+len(pdbgb.fns))
		for _, f := range *pdbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pdbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProfitDistributionBillSelect is the builder for selecting fields of ProfitDistributionBill entities.
type ProfitDistributionBillSelect struct {
	*ProfitDistributionBillQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pdbs *ProfitDistributionBillSelect) Aggregate(fns ...AggregateFunc) *ProfitDistributionBillSelect {
	pdbs.fns = append(pdbs.fns, fns...)
	return pdbs
}

// Scan applies the selector query and scans the result into the given value.
func (pdbs *ProfitDistributionBillSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pdbs.ctx, ent.OpQuerySelect)
	if err := pdbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProfitDistributionBillQuery, *ProfitDistributionBillSelect](ctx, pdbs.ProfitDistributionBillQuery, pdbs, pdbs.inters, v)
}

func (pdbs *ProfitDistributionBillSelect) sqlScan(ctx context.Context, root *ProfitDistributionBillQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pdbs.fns))
	for _, fn := range pdbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pdbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pdbs *ProfitDistributionBillSelect) Modify(modifiers ...func(s *sql.Selector)) *ProfitDistributionBillSelect {
	pdbs.modifiers = append(pdbs.modifiers, modifiers...)
	return pdbs
}
