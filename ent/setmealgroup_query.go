// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/product"
	"gitlab.jiguang.dev/pos-dine/dine/ent/setmealdetail"
	"gitlab.jiguang.dev/pos-dine/dine/ent/setmealgroup"
)

// SetMealGroupQuery is the builder for querying SetMealGroup entities.
type SetMealGroupQuery struct {
	config
	ctx         *QueryContext
	order       []setmealgroup.OrderOption
	inters      []Interceptor
	predicates  []predicate.SetMealGroup
	withProduct *ProductQuery
	withDetails *SetMealDetailQuery
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SetMealGroupQuery builder.
func (smgq *SetMealGroupQuery) Where(ps ...predicate.SetMealGroup) *SetMealGroupQuery {
	smgq.predicates = append(smgq.predicates, ps...)
	return smgq
}

// Limit the number of records to be returned by this query.
func (smgq *SetMealGroupQuery) Limit(limit int) *SetMealGroupQuery {
	smgq.ctx.Limit = &limit
	return smgq
}

// Offset to start from.
func (smgq *SetMealGroupQuery) Offset(offset int) *SetMealGroupQuery {
	smgq.ctx.Offset = &offset
	return smgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (smgq *SetMealGroupQuery) Unique(unique bool) *SetMealGroupQuery {
	smgq.ctx.Unique = &unique
	return smgq
}

// Order specifies how the records should be ordered.
func (smgq *SetMealGroupQuery) Order(o ...setmealgroup.OrderOption) *SetMealGroupQuery {
	smgq.order = append(smgq.order, o...)
	return smgq
}

// QueryProduct chains the current query on the "product" edge.
func (smgq *SetMealGroupQuery) QueryProduct() *ProductQuery {
	query := (&ProductClient{config: smgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := smgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := smgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(setmealgroup.Table, setmealgroup.FieldID, selector),
			sqlgraph.To(product.Table, product.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, setmealgroup.ProductTable, setmealgroup.ProductColumn),
		)
		fromU = sqlgraph.SetNeighbors(smgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDetails chains the current query on the "details" edge.
func (smgq *SetMealGroupQuery) QueryDetails() *SetMealDetailQuery {
	query := (&SetMealDetailClient{config: smgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := smgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := smgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(setmealgroup.Table, setmealgroup.FieldID, selector),
			sqlgraph.To(setmealdetail.Table, setmealdetail.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, setmealgroup.DetailsTable, setmealgroup.DetailsColumn),
		)
		fromU = sqlgraph.SetNeighbors(smgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SetMealGroup entity from the query.
// Returns a *NotFoundError when no SetMealGroup was found.
func (smgq *SetMealGroupQuery) First(ctx context.Context) (*SetMealGroup, error) {
	nodes, err := smgq.Limit(1).All(setContextOp(ctx, smgq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{setmealgroup.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (smgq *SetMealGroupQuery) FirstX(ctx context.Context) *SetMealGroup {
	node, err := smgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SetMealGroup ID from the query.
// Returns a *NotFoundError when no SetMealGroup ID was found.
func (smgq *SetMealGroupQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = smgq.Limit(1).IDs(setContextOp(ctx, smgq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{setmealgroup.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (smgq *SetMealGroupQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := smgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SetMealGroup entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SetMealGroup entity is found.
// Returns a *NotFoundError when no SetMealGroup entities are found.
func (smgq *SetMealGroupQuery) Only(ctx context.Context) (*SetMealGroup, error) {
	nodes, err := smgq.Limit(2).All(setContextOp(ctx, smgq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{setmealgroup.Label}
	default:
		return nil, &NotSingularError{setmealgroup.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (smgq *SetMealGroupQuery) OnlyX(ctx context.Context) *SetMealGroup {
	node, err := smgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SetMealGroup ID in the query.
// Returns a *NotSingularError when more than one SetMealGroup ID is found.
// Returns a *NotFoundError when no entities are found.
func (smgq *SetMealGroupQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = smgq.Limit(2).IDs(setContextOp(ctx, smgq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{setmealgroup.Label}
	default:
		err = &NotSingularError{setmealgroup.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (smgq *SetMealGroupQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := smgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SetMealGroups.
func (smgq *SetMealGroupQuery) All(ctx context.Context) ([]*SetMealGroup, error) {
	ctx = setContextOp(ctx, smgq.ctx, ent.OpQueryAll)
	if err := smgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SetMealGroup, *SetMealGroupQuery]()
	return withInterceptors[[]*SetMealGroup](ctx, smgq, qr, smgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (smgq *SetMealGroupQuery) AllX(ctx context.Context) []*SetMealGroup {
	nodes, err := smgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SetMealGroup IDs.
func (smgq *SetMealGroupQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if smgq.ctx.Unique == nil && smgq.path != nil {
		smgq.Unique(true)
	}
	ctx = setContextOp(ctx, smgq.ctx, ent.OpQueryIDs)
	if err = smgq.Select(setmealgroup.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (smgq *SetMealGroupQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := smgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (smgq *SetMealGroupQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, smgq.ctx, ent.OpQueryCount)
	if err := smgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, smgq, querierCount[*SetMealGroupQuery](), smgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (smgq *SetMealGroupQuery) CountX(ctx context.Context) int {
	count, err := smgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (smgq *SetMealGroupQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, smgq.ctx, ent.OpQueryExist)
	switch _, err := smgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (smgq *SetMealGroupQuery) ExistX(ctx context.Context) bool {
	exist, err := smgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SetMealGroupQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (smgq *SetMealGroupQuery) Clone() *SetMealGroupQuery {
	if smgq == nil {
		return nil
	}
	return &SetMealGroupQuery{
		config:      smgq.config,
		ctx:         smgq.ctx.Clone(),
		order:       append([]setmealgroup.OrderOption{}, smgq.order...),
		inters:      append([]Interceptor{}, smgq.inters...),
		predicates:  append([]predicate.SetMealGroup{}, smgq.predicates...),
		withProduct: smgq.withProduct.Clone(),
		withDetails: smgq.withDetails.Clone(),
		// clone intermediate query.
		sql:       smgq.sql.Clone(),
		path:      smgq.path,
		modifiers: append([]func(*sql.Selector){}, smgq.modifiers...),
	}
}

// WithProduct tells the query-builder to eager-load the nodes that are connected to
// the "product" edge. The optional arguments are used to configure the query builder of the edge.
func (smgq *SetMealGroupQuery) WithProduct(opts ...func(*ProductQuery)) *SetMealGroupQuery {
	query := (&ProductClient{config: smgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	smgq.withProduct = query
	return smgq
}

// WithDetails tells the query-builder to eager-load the nodes that are connected to
// the "details" edge. The optional arguments are used to configure the query builder of the edge.
func (smgq *SetMealGroupQuery) WithDetails(opts ...func(*SetMealDetailQuery)) *SetMealGroupQuery {
	query := (&SetMealDetailClient{config: smgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	smgq.withDetails = query
	return smgq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SetMealGroup.Query().
//		GroupBy(setmealgroup.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (smgq *SetMealGroupQuery) GroupBy(field string, fields ...string) *SetMealGroupGroupBy {
	smgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SetMealGroupGroupBy{build: smgq}
	grbuild.flds = &smgq.ctx.Fields
	grbuild.label = setmealgroup.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SetMealGroup.Query().
//		Select(setmealgroup.FieldCreatedAt).
//		Scan(ctx, &v)
func (smgq *SetMealGroupQuery) Select(fields ...string) *SetMealGroupSelect {
	smgq.ctx.Fields = append(smgq.ctx.Fields, fields...)
	sbuild := &SetMealGroupSelect{SetMealGroupQuery: smgq}
	sbuild.label = setmealgroup.Label
	sbuild.flds, sbuild.scan = &smgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SetMealGroupSelect configured with the given aggregations.
func (smgq *SetMealGroupQuery) Aggregate(fns ...AggregateFunc) *SetMealGroupSelect {
	return smgq.Select().Aggregate(fns...)
}

func (smgq *SetMealGroupQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range smgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, smgq); err != nil {
				return err
			}
		}
	}
	for _, f := range smgq.ctx.Fields {
		if !setmealgroup.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if smgq.path != nil {
		prev, err := smgq.path(ctx)
		if err != nil {
			return err
		}
		smgq.sql = prev
	}
	return nil
}

func (smgq *SetMealGroupQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SetMealGroup, error) {
	var (
		nodes       = []*SetMealGroup{}
		_spec       = smgq.querySpec()
		loadedTypes = [2]bool{
			smgq.withProduct != nil,
			smgq.withDetails != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SetMealGroup).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SetMealGroup{config: smgq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(smgq.modifiers) > 0 {
		_spec.Modifiers = smgq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, smgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := smgq.withProduct; query != nil {
		if err := smgq.loadProduct(ctx, query, nodes, nil,
			func(n *SetMealGroup, e *Product) { n.Edges.Product = e }); err != nil {
			return nil, err
		}
	}
	if query := smgq.withDetails; query != nil {
		if err := smgq.loadDetails(ctx, query, nodes,
			func(n *SetMealGroup) { n.Edges.Details = []*SetMealDetail{} },
			func(n *SetMealGroup, e *SetMealDetail) { n.Edges.Details = append(n.Edges.Details, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (smgq *SetMealGroupQuery) loadProduct(ctx context.Context, query *ProductQuery, nodes []*SetMealGroup, init func(*SetMealGroup), assign func(*SetMealGroup, *Product)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SetMealGroup)
	for i := range nodes {
		fk := nodes[i].ProductID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(product.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "product_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (smgq *SetMealGroupQuery) loadDetails(ctx context.Context, query *SetMealDetailQuery, nodes []*SetMealGroup, init func(*SetMealGroup), assign func(*SetMealGroup, *SetMealDetail)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*SetMealGroup)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(setmealdetail.FieldGroupID)
	}
	query.Where(predicate.SetMealDetail(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(setmealgroup.DetailsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.GroupID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "group_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (smgq *SetMealGroupQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := smgq.querySpec()
	if len(smgq.modifiers) > 0 {
		_spec.Modifiers = smgq.modifiers
	}
	_spec.Node.Columns = smgq.ctx.Fields
	if len(smgq.ctx.Fields) > 0 {
		_spec.Unique = smgq.ctx.Unique != nil && *smgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, smgq.driver, _spec)
}

func (smgq *SetMealGroupQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(setmealgroup.Table, setmealgroup.Columns, sqlgraph.NewFieldSpec(setmealgroup.FieldID, field.TypeUUID))
	_spec.From = smgq.sql
	if unique := smgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if smgq.path != nil {
		_spec.Unique = true
	}
	if fields := smgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, setmealgroup.FieldID)
		for i := range fields {
			if fields[i] != setmealgroup.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if smgq.withProduct != nil {
			_spec.Node.AddColumnOnce(setmealgroup.FieldProductID)
		}
	}
	if ps := smgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := smgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := smgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := smgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (smgq *SetMealGroupQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(smgq.driver.Dialect())
	t1 := builder.Table(setmealgroup.Table)
	columns := smgq.ctx.Fields
	if len(columns) == 0 {
		columns = setmealgroup.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if smgq.sql != nil {
		selector = smgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if smgq.ctx.Unique != nil && *smgq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range smgq.modifiers {
		m(selector)
	}
	for _, p := range smgq.predicates {
		p(selector)
	}
	for _, p := range smgq.order {
		p(selector)
	}
	if offset := smgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := smgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (smgq *SetMealGroupQuery) ForUpdate(opts ...sql.LockOption) *SetMealGroupQuery {
	if smgq.driver.Dialect() == dialect.Postgres {
		smgq.Unique(false)
	}
	smgq.modifiers = append(smgq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return smgq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (smgq *SetMealGroupQuery) ForShare(opts ...sql.LockOption) *SetMealGroupQuery {
	if smgq.driver.Dialect() == dialect.Postgres {
		smgq.Unique(false)
	}
	smgq.modifiers = append(smgq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return smgq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (smgq *SetMealGroupQuery) Modify(modifiers ...func(s *sql.Selector)) *SetMealGroupSelect {
	smgq.modifiers = append(smgq.modifiers, modifiers...)
	return smgq.Select()
}

// SetMealGroupGroupBy is the group-by builder for SetMealGroup entities.
type SetMealGroupGroupBy struct {
	selector
	build *SetMealGroupQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (smggb *SetMealGroupGroupBy) Aggregate(fns ...AggregateFunc) *SetMealGroupGroupBy {
	smggb.fns = append(smggb.fns, fns...)
	return smggb
}

// Scan applies the selector query and scans the result into the given value.
func (smggb *SetMealGroupGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, smggb.build.ctx, ent.OpQueryGroupBy)
	if err := smggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SetMealGroupQuery, *SetMealGroupGroupBy](ctx, smggb.build, smggb, smggb.build.inters, v)
}

func (smggb *SetMealGroupGroupBy) sqlScan(ctx context.Context, root *SetMealGroupQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(smggb.fns))
	for _, fn := range smggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*smggb.flds)+len(smggb.fns))
		for _, f := range *smggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*smggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := smggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SetMealGroupSelect is the builder for selecting fields of SetMealGroup entities.
type SetMealGroupSelect struct {
	*SetMealGroupQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (smgs *SetMealGroupSelect) Aggregate(fns ...AggregateFunc) *SetMealGroupSelect {
	smgs.fns = append(smgs.fns, fns...)
	return smgs
}

// Scan applies the selector query and scans the result into the given value.
func (smgs *SetMealGroupSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, smgs.ctx, ent.OpQuerySelect)
	if err := smgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SetMealGroupQuery, *SetMealGroupSelect](ctx, smgs.SetMealGroupQuery, smgs, smgs.inters, v)
}

func (smgs *SetMealGroupSelect) sqlScan(ctx context.Context, root *SetMealGroupQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(smgs.fns))
	for _, fn := range smgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*smgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := smgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (smgs *SetMealGroupSelect) Modify(modifiers ...func(s *sql.Selector)) *SetMealGroupSelect {
	smgs.modifiers = append(smgs.modifiers, modifiers...)
	return smgs
}
