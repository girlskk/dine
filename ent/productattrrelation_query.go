// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/product"
	"gitlab.jiguang.dev/pos-dine/dine/ent/productattr"
	"gitlab.jiguang.dev/pos-dine/dine/ent/productattritem"
	"gitlab.jiguang.dev/pos-dine/dine/ent/productattrrelation"
)

// ProductAttrRelationQuery is the builder for querying ProductAttrRelation entities.
type ProductAttrRelationQuery struct {
	config
	ctx          *QueryContext
	order        []productattrrelation.OrderOption
	inters       []Interceptor
	predicates   []predicate.ProductAttrRelation
	withProduct  *ProductQuery
	withAttr     *ProductAttrQuery
	withAttrItem *ProductAttrItemQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProductAttrRelationQuery builder.
func (parq *ProductAttrRelationQuery) Where(ps ...predicate.ProductAttrRelation) *ProductAttrRelationQuery {
	parq.predicates = append(parq.predicates, ps...)
	return parq
}

// Limit the number of records to be returned by this query.
func (parq *ProductAttrRelationQuery) Limit(limit int) *ProductAttrRelationQuery {
	parq.ctx.Limit = &limit
	return parq
}

// Offset to start from.
func (parq *ProductAttrRelationQuery) Offset(offset int) *ProductAttrRelationQuery {
	parq.ctx.Offset = &offset
	return parq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (parq *ProductAttrRelationQuery) Unique(unique bool) *ProductAttrRelationQuery {
	parq.ctx.Unique = &unique
	return parq
}

// Order specifies how the records should be ordered.
func (parq *ProductAttrRelationQuery) Order(o ...productattrrelation.OrderOption) *ProductAttrRelationQuery {
	parq.order = append(parq.order, o...)
	return parq
}

// QueryProduct chains the current query on the "product" edge.
func (parq *ProductAttrRelationQuery) QueryProduct() *ProductQuery {
	query := (&ProductClient{config: parq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := parq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := parq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productattrrelation.Table, productattrrelation.FieldID, selector),
			sqlgraph.To(product.Table, product.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, productattrrelation.ProductTable, productattrrelation.ProductColumn),
		)
		fromU = sqlgraph.SetNeighbors(parq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAttr chains the current query on the "attr" edge.
func (parq *ProductAttrRelationQuery) QueryAttr() *ProductAttrQuery {
	query := (&ProductAttrClient{config: parq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := parq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := parq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productattrrelation.Table, productattrrelation.FieldID, selector),
			sqlgraph.To(productattr.Table, productattr.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, productattrrelation.AttrTable, productattrrelation.AttrColumn),
		)
		fromU = sqlgraph.SetNeighbors(parq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAttrItem chains the current query on the "attr_item" edge.
func (parq *ProductAttrRelationQuery) QueryAttrItem() *ProductAttrItemQuery {
	query := (&ProductAttrItemClient{config: parq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := parq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := parq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productattrrelation.Table, productattrrelation.FieldID, selector),
			sqlgraph.To(productattritem.Table, productattritem.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, productattrrelation.AttrItemTable, productattrrelation.AttrItemColumn),
		)
		fromU = sqlgraph.SetNeighbors(parq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProductAttrRelation entity from the query.
// Returns a *NotFoundError when no ProductAttrRelation was found.
func (parq *ProductAttrRelationQuery) First(ctx context.Context) (*ProductAttrRelation, error) {
	nodes, err := parq.Limit(1).All(setContextOp(ctx, parq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{productattrrelation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) FirstX(ctx context.Context) *ProductAttrRelation {
	node, err := parq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProductAttrRelation ID from the query.
// Returns a *NotFoundError when no ProductAttrRelation ID was found.
func (parq *ProductAttrRelationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = parq.Limit(1).IDs(setContextOp(ctx, parq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{productattrrelation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := parq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProductAttrRelation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProductAttrRelation entity is found.
// Returns a *NotFoundError when no ProductAttrRelation entities are found.
func (parq *ProductAttrRelationQuery) Only(ctx context.Context) (*ProductAttrRelation, error) {
	nodes, err := parq.Limit(2).All(setContextOp(ctx, parq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{productattrrelation.Label}
	default:
		return nil, &NotSingularError{productattrrelation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) OnlyX(ctx context.Context) *ProductAttrRelation {
	node, err := parq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProductAttrRelation ID in the query.
// Returns a *NotSingularError when more than one ProductAttrRelation ID is found.
// Returns a *NotFoundError when no entities are found.
func (parq *ProductAttrRelationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = parq.Limit(2).IDs(setContextOp(ctx, parq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{productattrrelation.Label}
	default:
		err = &NotSingularError{productattrrelation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := parq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProductAttrRelations.
func (parq *ProductAttrRelationQuery) All(ctx context.Context) ([]*ProductAttrRelation, error) {
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryAll)
	if err := parq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProductAttrRelation, *ProductAttrRelationQuery]()
	return withInterceptors[[]*ProductAttrRelation](ctx, parq, qr, parq.inters)
}

// AllX is like All, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) AllX(ctx context.Context) []*ProductAttrRelation {
	nodes, err := parq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProductAttrRelation IDs.
func (parq *ProductAttrRelationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if parq.ctx.Unique == nil && parq.path != nil {
		parq.Unique(true)
	}
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryIDs)
	if err = parq.Select(productattrrelation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := parq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (parq *ProductAttrRelationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryCount)
	if err := parq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, parq, querierCount[*ProductAttrRelationQuery](), parq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) CountX(ctx context.Context) int {
	count, err := parq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (parq *ProductAttrRelationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, parq.ctx, ent.OpQueryExist)
	switch _, err := parq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (parq *ProductAttrRelationQuery) ExistX(ctx context.Context) bool {
	exist, err := parq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProductAttrRelationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (parq *ProductAttrRelationQuery) Clone() *ProductAttrRelationQuery {
	if parq == nil {
		return nil
	}
	return &ProductAttrRelationQuery{
		config:       parq.config,
		ctx:          parq.ctx.Clone(),
		order:        append([]productattrrelation.OrderOption{}, parq.order...),
		inters:       append([]Interceptor{}, parq.inters...),
		predicates:   append([]predicate.ProductAttrRelation{}, parq.predicates...),
		withProduct:  parq.withProduct.Clone(),
		withAttr:     parq.withAttr.Clone(),
		withAttrItem: parq.withAttrItem.Clone(),
		// clone intermediate query.
		sql:       parq.sql.Clone(),
		path:      parq.path,
		modifiers: append([]func(*sql.Selector){}, parq.modifiers...),
	}
}

// WithProduct tells the query-builder to eager-load the nodes that are connected to
// the "product" edge. The optional arguments are used to configure the query builder of the edge.
func (parq *ProductAttrRelationQuery) WithProduct(opts ...func(*ProductQuery)) *ProductAttrRelationQuery {
	query := (&ProductClient{config: parq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	parq.withProduct = query
	return parq
}

// WithAttr tells the query-builder to eager-load the nodes that are connected to
// the "attr" edge. The optional arguments are used to configure the query builder of the edge.
func (parq *ProductAttrRelationQuery) WithAttr(opts ...func(*ProductAttrQuery)) *ProductAttrRelationQuery {
	query := (&ProductAttrClient{config: parq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	parq.withAttr = query
	return parq
}

// WithAttrItem tells the query-builder to eager-load the nodes that are connected to
// the "attr_item" edge. The optional arguments are used to configure the query builder of the edge.
func (parq *ProductAttrRelationQuery) WithAttrItem(opts ...func(*ProductAttrItemQuery)) *ProductAttrRelationQuery {
	query := (&ProductAttrItemClient{config: parq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	parq.withAttrItem = query
	return parq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProductAttrRelation.Query().
//		GroupBy(productattrrelation.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (parq *ProductAttrRelationQuery) GroupBy(field string, fields ...string) *ProductAttrRelationGroupBy {
	parq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProductAttrRelationGroupBy{build: parq}
	grbuild.flds = &parq.ctx.Fields
	grbuild.label = productattrrelation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProductAttrRelation.Query().
//		Select(productattrrelation.FieldCreatedAt).
//		Scan(ctx, &v)
func (parq *ProductAttrRelationQuery) Select(fields ...string) *ProductAttrRelationSelect {
	parq.ctx.Fields = append(parq.ctx.Fields, fields...)
	sbuild := &ProductAttrRelationSelect{ProductAttrRelationQuery: parq}
	sbuild.label = productattrrelation.Label
	sbuild.flds, sbuild.scan = &parq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProductAttrRelationSelect configured with the given aggregations.
func (parq *ProductAttrRelationQuery) Aggregate(fns ...AggregateFunc) *ProductAttrRelationSelect {
	return parq.Select().Aggregate(fns...)
}

func (parq *ProductAttrRelationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range parq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, parq); err != nil {
				return err
			}
		}
	}
	for _, f := range parq.ctx.Fields {
		if !productattrrelation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if parq.path != nil {
		prev, err := parq.path(ctx)
		if err != nil {
			return err
		}
		parq.sql = prev
	}
	return nil
}

func (parq *ProductAttrRelationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProductAttrRelation, error) {
	var (
		nodes       = []*ProductAttrRelation{}
		_spec       = parq.querySpec()
		loadedTypes = [3]bool{
			parq.withProduct != nil,
			parq.withAttr != nil,
			parq.withAttrItem != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProductAttrRelation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProductAttrRelation{config: parq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(parq.modifiers) > 0 {
		_spec.Modifiers = parq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, parq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := parq.withProduct; query != nil {
		if err := parq.loadProduct(ctx, query, nodes, nil,
			func(n *ProductAttrRelation, e *Product) { n.Edges.Product = e }); err != nil {
			return nil, err
		}
	}
	if query := parq.withAttr; query != nil {
		if err := parq.loadAttr(ctx, query, nodes, nil,
			func(n *ProductAttrRelation, e *ProductAttr) { n.Edges.Attr = e }); err != nil {
			return nil, err
		}
	}
	if query := parq.withAttrItem; query != nil {
		if err := parq.loadAttrItem(ctx, query, nodes, nil,
			func(n *ProductAttrRelation, e *ProductAttrItem) { n.Edges.AttrItem = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (parq *ProductAttrRelationQuery) loadProduct(ctx context.Context, query *ProductQuery, nodes []*ProductAttrRelation, init func(*ProductAttrRelation), assign func(*ProductAttrRelation, *Product)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProductAttrRelation)
	for i := range nodes {
		fk := nodes[i].ProductID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(product.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "product_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (parq *ProductAttrRelationQuery) loadAttr(ctx context.Context, query *ProductAttrQuery, nodes []*ProductAttrRelation, init func(*ProductAttrRelation), assign func(*ProductAttrRelation, *ProductAttr)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProductAttrRelation)
	for i := range nodes {
		fk := nodes[i].AttrID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(productattr.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "attr_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (parq *ProductAttrRelationQuery) loadAttrItem(ctx context.Context, query *ProductAttrItemQuery, nodes []*ProductAttrRelation, init func(*ProductAttrRelation), assign func(*ProductAttrRelation, *ProductAttrItem)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProductAttrRelation)
	for i := range nodes {
		fk := nodes[i].AttrItemID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(productattritem.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "attr_item_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (parq *ProductAttrRelationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := parq.querySpec()
	if len(parq.modifiers) > 0 {
		_spec.Modifiers = parq.modifiers
	}
	_spec.Node.Columns = parq.ctx.Fields
	if len(parq.ctx.Fields) > 0 {
		_spec.Unique = parq.ctx.Unique != nil && *parq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, parq.driver, _spec)
}

func (parq *ProductAttrRelationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(productattrrelation.Table, productattrrelation.Columns, sqlgraph.NewFieldSpec(productattrrelation.FieldID, field.TypeUUID))
	_spec.From = parq.sql
	if unique := parq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if parq.path != nil {
		_spec.Unique = true
	}
	if fields := parq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, productattrrelation.FieldID)
		for i := range fields {
			if fields[i] != productattrrelation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if parq.withProduct != nil {
			_spec.Node.AddColumnOnce(productattrrelation.FieldProductID)
		}
		if parq.withAttr != nil {
			_spec.Node.AddColumnOnce(productattrrelation.FieldAttrID)
		}
		if parq.withAttrItem != nil {
			_spec.Node.AddColumnOnce(productattrrelation.FieldAttrItemID)
		}
	}
	if ps := parq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := parq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := parq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := parq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (parq *ProductAttrRelationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(parq.driver.Dialect())
	t1 := builder.Table(productattrrelation.Table)
	columns := parq.ctx.Fields
	if len(columns) == 0 {
		columns = productattrrelation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if parq.sql != nil {
		selector = parq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if parq.ctx.Unique != nil && *parq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range parq.modifiers {
		m(selector)
	}
	for _, p := range parq.predicates {
		p(selector)
	}
	for _, p := range parq.order {
		p(selector)
	}
	if offset := parq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := parq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (parq *ProductAttrRelationQuery) ForUpdate(opts ...sql.LockOption) *ProductAttrRelationQuery {
	if parq.driver.Dialect() == dialect.Postgres {
		parq.Unique(false)
	}
	parq.modifiers = append(parq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return parq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (parq *ProductAttrRelationQuery) ForShare(opts ...sql.LockOption) *ProductAttrRelationQuery {
	if parq.driver.Dialect() == dialect.Postgres {
		parq.Unique(false)
	}
	parq.modifiers = append(parq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return parq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (parq *ProductAttrRelationQuery) Modify(modifiers ...func(s *sql.Selector)) *ProductAttrRelationSelect {
	parq.modifiers = append(parq.modifiers, modifiers...)
	return parq.Select()
}

// ProductAttrRelationGroupBy is the group-by builder for ProductAttrRelation entities.
type ProductAttrRelationGroupBy struct {
	selector
	build *ProductAttrRelationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pargb *ProductAttrRelationGroupBy) Aggregate(fns ...AggregateFunc) *ProductAttrRelationGroupBy {
	pargb.fns = append(pargb.fns, fns...)
	return pargb
}

// Scan applies the selector query and scans the result into the given value.
func (pargb *ProductAttrRelationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pargb.build.ctx, ent.OpQueryGroupBy)
	if err := pargb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductAttrRelationQuery, *ProductAttrRelationGroupBy](ctx, pargb.build, pargb, pargb.build.inters, v)
}

func (pargb *ProductAttrRelationGroupBy) sqlScan(ctx context.Context, root *ProductAttrRelationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pargb.fns))
	for _, fn := range pargb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pargb.flds)+len(pargb.fns))
		for _, f := range *pargb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pargb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pargb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProductAttrRelationSelect is the builder for selecting fields of ProductAttrRelation entities.
type ProductAttrRelationSelect struct {
	*ProductAttrRelationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pars *ProductAttrRelationSelect) Aggregate(fns ...AggregateFunc) *ProductAttrRelationSelect {
	pars.fns = append(pars.fns, fns...)
	return pars
}

// Scan applies the selector query and scans the result into the given value.
func (pars *ProductAttrRelationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pars.ctx, ent.OpQuerySelect)
	if err := pars.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductAttrRelationQuery, *ProductAttrRelationSelect](ctx, pars.ProductAttrRelationQuery, pars, pars.inters, v)
}

func (pars *ProductAttrRelationSelect) sqlScan(ctx context.Context, root *ProductAttrRelationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pars.fns))
	for _, fn := range pars.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pars.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pars.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pars *ProductAttrRelationSelect) Modify(modifiers ...func(s *sql.Selector)) *ProductAttrRelationSelect {
	pars.modifiers = append(pars.modifiers, modifiers...)
	return pars
}
