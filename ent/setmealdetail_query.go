// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/product"
	"gitlab.jiguang.dev/pos-dine/dine/ent/setmealdetail"
	"gitlab.jiguang.dev/pos-dine/dine/ent/setmealgroup"
)

// SetMealDetailQuery is the builder for querying SetMealDetail entities.
type SetMealDetailQuery struct {
	config
	ctx         *QueryContext
	order       []setmealdetail.OrderOption
	inters      []Interceptor
	predicates  []predicate.SetMealDetail
	withGroup   *SetMealGroupQuery
	withProduct *ProductQuery
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SetMealDetailQuery builder.
func (smdq *SetMealDetailQuery) Where(ps ...predicate.SetMealDetail) *SetMealDetailQuery {
	smdq.predicates = append(smdq.predicates, ps...)
	return smdq
}

// Limit the number of records to be returned by this query.
func (smdq *SetMealDetailQuery) Limit(limit int) *SetMealDetailQuery {
	smdq.ctx.Limit = &limit
	return smdq
}

// Offset to start from.
func (smdq *SetMealDetailQuery) Offset(offset int) *SetMealDetailQuery {
	smdq.ctx.Offset = &offset
	return smdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (smdq *SetMealDetailQuery) Unique(unique bool) *SetMealDetailQuery {
	smdq.ctx.Unique = &unique
	return smdq
}

// Order specifies how the records should be ordered.
func (smdq *SetMealDetailQuery) Order(o ...setmealdetail.OrderOption) *SetMealDetailQuery {
	smdq.order = append(smdq.order, o...)
	return smdq
}

// QueryGroup chains the current query on the "group" edge.
func (smdq *SetMealDetailQuery) QueryGroup() *SetMealGroupQuery {
	query := (&SetMealGroupClient{config: smdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := smdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := smdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(setmealdetail.Table, setmealdetail.FieldID, selector),
			sqlgraph.To(setmealgroup.Table, setmealgroup.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, setmealdetail.GroupTable, setmealdetail.GroupColumn),
		)
		fromU = sqlgraph.SetNeighbors(smdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProduct chains the current query on the "product" edge.
func (smdq *SetMealDetailQuery) QueryProduct() *ProductQuery {
	query := (&ProductClient{config: smdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := smdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := smdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(setmealdetail.Table, setmealdetail.FieldID, selector),
			sqlgraph.To(product.Table, product.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, setmealdetail.ProductTable, setmealdetail.ProductColumn),
		)
		fromU = sqlgraph.SetNeighbors(smdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SetMealDetail entity from the query.
// Returns a *NotFoundError when no SetMealDetail was found.
func (smdq *SetMealDetailQuery) First(ctx context.Context) (*SetMealDetail, error) {
	nodes, err := smdq.Limit(1).All(setContextOp(ctx, smdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{setmealdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (smdq *SetMealDetailQuery) FirstX(ctx context.Context) *SetMealDetail {
	node, err := smdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SetMealDetail ID from the query.
// Returns a *NotFoundError when no SetMealDetail ID was found.
func (smdq *SetMealDetailQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = smdq.Limit(1).IDs(setContextOp(ctx, smdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{setmealdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (smdq *SetMealDetailQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := smdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SetMealDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SetMealDetail entity is found.
// Returns a *NotFoundError when no SetMealDetail entities are found.
func (smdq *SetMealDetailQuery) Only(ctx context.Context) (*SetMealDetail, error) {
	nodes, err := smdq.Limit(2).All(setContextOp(ctx, smdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{setmealdetail.Label}
	default:
		return nil, &NotSingularError{setmealdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (smdq *SetMealDetailQuery) OnlyX(ctx context.Context) *SetMealDetail {
	node, err := smdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SetMealDetail ID in the query.
// Returns a *NotSingularError when more than one SetMealDetail ID is found.
// Returns a *NotFoundError when no entities are found.
func (smdq *SetMealDetailQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = smdq.Limit(2).IDs(setContextOp(ctx, smdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{setmealdetail.Label}
	default:
		err = &NotSingularError{setmealdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (smdq *SetMealDetailQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := smdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SetMealDetails.
func (smdq *SetMealDetailQuery) All(ctx context.Context) ([]*SetMealDetail, error) {
	ctx = setContextOp(ctx, smdq.ctx, ent.OpQueryAll)
	if err := smdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SetMealDetail, *SetMealDetailQuery]()
	return withInterceptors[[]*SetMealDetail](ctx, smdq, qr, smdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (smdq *SetMealDetailQuery) AllX(ctx context.Context) []*SetMealDetail {
	nodes, err := smdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SetMealDetail IDs.
func (smdq *SetMealDetailQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if smdq.ctx.Unique == nil && smdq.path != nil {
		smdq.Unique(true)
	}
	ctx = setContextOp(ctx, smdq.ctx, ent.OpQueryIDs)
	if err = smdq.Select(setmealdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (smdq *SetMealDetailQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := smdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (smdq *SetMealDetailQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, smdq.ctx, ent.OpQueryCount)
	if err := smdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, smdq, querierCount[*SetMealDetailQuery](), smdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (smdq *SetMealDetailQuery) CountX(ctx context.Context) int {
	count, err := smdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (smdq *SetMealDetailQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, smdq.ctx, ent.OpQueryExist)
	switch _, err := smdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (smdq *SetMealDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := smdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SetMealDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (smdq *SetMealDetailQuery) Clone() *SetMealDetailQuery {
	if smdq == nil {
		return nil
	}
	return &SetMealDetailQuery{
		config:      smdq.config,
		ctx:         smdq.ctx.Clone(),
		order:       append([]setmealdetail.OrderOption{}, smdq.order...),
		inters:      append([]Interceptor{}, smdq.inters...),
		predicates:  append([]predicate.SetMealDetail{}, smdq.predicates...),
		withGroup:   smdq.withGroup.Clone(),
		withProduct: smdq.withProduct.Clone(),
		// clone intermediate query.
		sql:       smdq.sql.Clone(),
		path:      smdq.path,
		modifiers: append([]func(*sql.Selector){}, smdq.modifiers...),
	}
}

// WithGroup tells the query-builder to eager-load the nodes that are connected to
// the "group" edge. The optional arguments are used to configure the query builder of the edge.
func (smdq *SetMealDetailQuery) WithGroup(opts ...func(*SetMealGroupQuery)) *SetMealDetailQuery {
	query := (&SetMealGroupClient{config: smdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	smdq.withGroup = query
	return smdq
}

// WithProduct tells the query-builder to eager-load the nodes that are connected to
// the "product" edge. The optional arguments are used to configure the query builder of the edge.
func (smdq *SetMealDetailQuery) WithProduct(opts ...func(*ProductQuery)) *SetMealDetailQuery {
	query := (&ProductClient{config: smdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	smdq.withProduct = query
	return smdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SetMealDetail.Query().
//		GroupBy(setmealdetail.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (smdq *SetMealDetailQuery) GroupBy(field string, fields ...string) *SetMealDetailGroupBy {
	smdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SetMealDetailGroupBy{build: smdq}
	grbuild.flds = &smdq.ctx.Fields
	grbuild.label = setmealdetail.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SetMealDetail.Query().
//		Select(setmealdetail.FieldCreatedAt).
//		Scan(ctx, &v)
func (smdq *SetMealDetailQuery) Select(fields ...string) *SetMealDetailSelect {
	smdq.ctx.Fields = append(smdq.ctx.Fields, fields...)
	sbuild := &SetMealDetailSelect{SetMealDetailQuery: smdq}
	sbuild.label = setmealdetail.Label
	sbuild.flds, sbuild.scan = &smdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SetMealDetailSelect configured with the given aggregations.
func (smdq *SetMealDetailQuery) Aggregate(fns ...AggregateFunc) *SetMealDetailSelect {
	return smdq.Select().Aggregate(fns...)
}

func (smdq *SetMealDetailQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range smdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, smdq); err != nil {
				return err
			}
		}
	}
	for _, f := range smdq.ctx.Fields {
		if !setmealdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if smdq.path != nil {
		prev, err := smdq.path(ctx)
		if err != nil {
			return err
		}
		smdq.sql = prev
	}
	return nil
}

func (smdq *SetMealDetailQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SetMealDetail, error) {
	var (
		nodes       = []*SetMealDetail{}
		_spec       = smdq.querySpec()
		loadedTypes = [2]bool{
			smdq.withGroup != nil,
			smdq.withProduct != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SetMealDetail).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SetMealDetail{config: smdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(smdq.modifiers) > 0 {
		_spec.Modifiers = smdq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, smdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := smdq.withGroup; query != nil {
		if err := smdq.loadGroup(ctx, query, nodes, nil,
			func(n *SetMealDetail, e *SetMealGroup) { n.Edges.Group = e }); err != nil {
			return nil, err
		}
	}
	if query := smdq.withProduct; query != nil {
		if err := smdq.loadProduct(ctx, query, nodes, nil,
			func(n *SetMealDetail, e *Product) { n.Edges.Product = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (smdq *SetMealDetailQuery) loadGroup(ctx context.Context, query *SetMealGroupQuery, nodes []*SetMealDetail, init func(*SetMealDetail), assign func(*SetMealDetail, *SetMealGroup)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SetMealDetail)
	for i := range nodes {
		fk := nodes[i].GroupID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(setmealgroup.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "group_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (smdq *SetMealDetailQuery) loadProduct(ctx context.Context, query *ProductQuery, nodes []*SetMealDetail, init func(*SetMealDetail), assign func(*SetMealDetail, *Product)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SetMealDetail)
	for i := range nodes {
		fk := nodes[i].ProductID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(product.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "product_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (smdq *SetMealDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := smdq.querySpec()
	if len(smdq.modifiers) > 0 {
		_spec.Modifiers = smdq.modifiers
	}
	_spec.Node.Columns = smdq.ctx.Fields
	if len(smdq.ctx.Fields) > 0 {
		_spec.Unique = smdq.ctx.Unique != nil && *smdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, smdq.driver, _spec)
}

func (smdq *SetMealDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(setmealdetail.Table, setmealdetail.Columns, sqlgraph.NewFieldSpec(setmealdetail.FieldID, field.TypeUUID))
	_spec.From = smdq.sql
	if unique := smdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if smdq.path != nil {
		_spec.Unique = true
	}
	if fields := smdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, setmealdetail.FieldID)
		for i := range fields {
			if fields[i] != setmealdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if smdq.withGroup != nil {
			_spec.Node.AddColumnOnce(setmealdetail.FieldGroupID)
		}
		if smdq.withProduct != nil {
			_spec.Node.AddColumnOnce(setmealdetail.FieldProductID)
		}
	}
	if ps := smdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := smdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := smdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := smdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (smdq *SetMealDetailQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(smdq.driver.Dialect())
	t1 := builder.Table(setmealdetail.Table)
	columns := smdq.ctx.Fields
	if len(columns) == 0 {
		columns = setmealdetail.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if smdq.sql != nil {
		selector = smdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if smdq.ctx.Unique != nil && *smdq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range smdq.modifiers {
		m(selector)
	}
	for _, p := range smdq.predicates {
		p(selector)
	}
	for _, p := range smdq.order {
		p(selector)
	}
	if offset := smdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := smdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (smdq *SetMealDetailQuery) ForUpdate(opts ...sql.LockOption) *SetMealDetailQuery {
	if smdq.driver.Dialect() == dialect.Postgres {
		smdq.Unique(false)
	}
	smdq.modifiers = append(smdq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return smdq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (smdq *SetMealDetailQuery) ForShare(opts ...sql.LockOption) *SetMealDetailQuery {
	if smdq.driver.Dialect() == dialect.Postgres {
		smdq.Unique(false)
	}
	smdq.modifiers = append(smdq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return smdq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (smdq *SetMealDetailQuery) Modify(modifiers ...func(s *sql.Selector)) *SetMealDetailSelect {
	smdq.modifiers = append(smdq.modifiers, modifiers...)
	return smdq.Select()
}

// SetMealDetailGroupBy is the group-by builder for SetMealDetail entities.
type SetMealDetailGroupBy struct {
	selector
	build *SetMealDetailQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (smdgb *SetMealDetailGroupBy) Aggregate(fns ...AggregateFunc) *SetMealDetailGroupBy {
	smdgb.fns = append(smdgb.fns, fns...)
	return smdgb
}

// Scan applies the selector query and scans the result into the given value.
func (smdgb *SetMealDetailGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, smdgb.build.ctx, ent.OpQueryGroupBy)
	if err := smdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SetMealDetailQuery, *SetMealDetailGroupBy](ctx, smdgb.build, smdgb, smdgb.build.inters, v)
}

func (smdgb *SetMealDetailGroupBy) sqlScan(ctx context.Context, root *SetMealDetailQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(smdgb.fns))
	for _, fn := range smdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*smdgb.flds)+len(smdgb.fns))
		for _, f := range *smdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*smdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := smdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SetMealDetailSelect is the builder for selecting fields of SetMealDetail entities.
type SetMealDetailSelect struct {
	*SetMealDetailQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (smds *SetMealDetailSelect) Aggregate(fns ...AggregateFunc) *SetMealDetailSelect {
	smds.fns = append(smds.fns, fns...)
	return smds
}

// Scan applies the selector query and scans the result into the given value.
func (smds *SetMealDetailSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, smds.ctx, ent.OpQuerySelect)
	if err := smds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SetMealDetailQuery, *SetMealDetailSelect](ctx, smds.SetMealDetailQuery, smds, smds.inters, v)
}

func (smds *SetMealDetailSelect) sqlScan(ctx context.Context, root *SetMealDetailQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(smds.fns))
	for _, fn := range smds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*smds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := smds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (smds *SetMealDetailSelect) Modify(modifiers ...func(s *sql.Selector)) *SetMealDetailSelect {
	smds.modifiers = append(smds.modifiers, modifiers...)
	return smds
}
