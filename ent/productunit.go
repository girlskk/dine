// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/productunit"
)

// ProductUnit is the model entity for the ProductUnit schema.
type ProductUnit struct {
	config `json:"-"`
	// ID of the ent.
	// UUID as primary key
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 单位名称
	Name string `json:"name,omitempty"`
	// 单位类型：quantity（数量单位）、weight（重量单位）
	Type domain.ProductUnitType `json:"type,omitempty"`
	// 品牌商ID
	MerchantID uuid.UUID `json:"merchant_id,omitempty"`
	// 门店ID
	StoreID uuid.UUID `json:"store_id,omitempty"`
	// 关联的商品数量
	ProductCount int `json:"product_count,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProductUnitQuery when eager-loading is set.
	Edges        ProductUnitEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProductUnitEdges holds the relations/edges for other nodes in the graph.
type ProductUnitEdges struct {
	// 关联的商品
	Products []*Product `json:"products,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProductsOrErr returns the Products value or an error if the edge
// was not loaded in eager-loading.
func (e ProductUnitEdges) ProductsOrErr() ([]*Product, error) {
	if e.loadedTypes[0] {
		return e.Products, nil
	}
	return nil, &NotLoadedError{edge: "products"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductUnit) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case productunit.FieldDeletedAt, productunit.FieldProductCount:
			values[i] = new(sql.NullInt64)
		case productunit.FieldName, productunit.FieldType:
			values[i] = new(sql.NullString)
		case productunit.FieldCreatedAt, productunit.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case productunit.FieldID, productunit.FieldMerchantID, productunit.FieldStoreID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductUnit fields.
func (pu *ProductUnit) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productunit.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pu.ID = *value
			}
		case productunit.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pu.CreatedAt = value.Time
			}
		case productunit.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pu.UpdatedAt = value.Time
			}
		case productunit.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				pu.DeletedAt = value.Int64
			}
		case productunit.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pu.Name = value.String
			}
		case productunit.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				pu.Type = domain.ProductUnitType(value.String)
			}
		case productunit.FieldMerchantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field merchant_id", values[i])
			} else if value != nil {
				pu.MerchantID = *value
			}
		case productunit.FieldStoreID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value != nil {
				pu.StoreID = *value
			}
		case productunit.FieldProductCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_count", values[i])
			} else if value.Valid {
				pu.ProductCount = int(value.Int64)
			}
		default:
			pu.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductUnit.
// This includes values selected through modifiers, order, etc.
func (pu *ProductUnit) Value(name string) (ent.Value, error) {
	return pu.selectValues.Get(name)
}

// QueryProducts queries the "products" edge of the ProductUnit entity.
func (pu *ProductUnit) QueryProducts() *ProductQuery {
	return NewProductUnitClient(pu.config).QueryProducts(pu)
}

// Update returns a builder for updating this ProductUnit.
// Note that you need to call ProductUnit.Unwrap() before calling this method if this ProductUnit
// was returned from a transaction, and the transaction was committed or rolled back.
func (pu *ProductUnit) Update() *ProductUnitUpdateOne {
	return NewProductUnitClient(pu.config).UpdateOne(pu)
}

// Unwrap unwraps the ProductUnit entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pu *ProductUnit) Unwrap() *ProductUnit {
	_tx, ok := pu.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductUnit is not a transactional entity")
	}
	pu.config.driver = _tx.drv
	return pu
}

// String implements the fmt.Stringer.
func (pu *ProductUnit) String() string {
	var builder strings.Builder
	builder.WriteString("ProductUnit(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pu.ID))
	builder.WriteString("created_at=")
	builder.WriteString(pu.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pu.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", pu.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(pu.Name)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", pu.Type))
	builder.WriteString(", ")
	builder.WriteString("merchant_id=")
	builder.WriteString(fmt.Sprintf("%v", pu.MerchantID))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", pu.StoreID))
	builder.WriteString(", ")
	builder.WriteString("product_count=")
	builder.WriteString(fmt.Sprintf("%v", pu.ProductCount))
	builder.WriteByte(')')
	return builder.String()
}

// ProductUnits is a parsable slice of ProductUnit.
type ProductUnits []*ProductUnit
