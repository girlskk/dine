// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/reconciliationrecord"
)

// ReconciliationRecord is the model entity for the ReconciliationRecord schema.
type ReconciliationRecord struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 单号
	No string `json:"no,omitempty"`
	// 门店ID
	StoreID int `json:"store_id,omitempty"`
	// 门店名称
	StoreName string `json:"store_name,omitempty"`
	// 入账笔数
	OrderCount int `json:"order_count,omitempty"`
	// 入账金额
	Amount decimal.Decimal `json:"amount,omitempty"`
	// 支付渠道
	Channel domain.OrderPaidChannel `json:"channel,omitempty"`
	// 账单日期
	Date         time.Time `json:"date,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReconciliationRecord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case reconciliationrecord.FieldAmount:
			values[i] = new(decimal.Decimal)
		case reconciliationrecord.FieldID, reconciliationrecord.FieldStoreID, reconciliationrecord.FieldOrderCount:
			values[i] = new(sql.NullInt64)
		case reconciliationrecord.FieldNo, reconciliationrecord.FieldStoreName, reconciliationrecord.FieldChannel:
			values[i] = new(sql.NullString)
		case reconciliationrecord.FieldCreatedAt, reconciliationrecord.FieldUpdatedAt, reconciliationrecord.FieldDate:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReconciliationRecord fields.
func (rr *ReconciliationRecord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case reconciliationrecord.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rr.ID = int(value.Int64)
		case reconciliationrecord.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rr.CreatedAt = value.Time
			}
		case reconciliationrecord.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rr.UpdatedAt = value.Time
			}
		case reconciliationrecord.FieldNo:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field no", values[i])
			} else if value.Valid {
				rr.No = value.String
			}
		case reconciliationrecord.FieldStoreID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value.Valid {
				rr.StoreID = int(value.Int64)
			}
		case reconciliationrecord.FieldStoreName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field store_name", values[i])
			} else if value.Valid {
				rr.StoreName = value.String
			}
		case reconciliationrecord.FieldOrderCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field order_count", values[i])
			} else if value.Valid {
				rr.OrderCount = int(value.Int64)
			}
		case reconciliationrecord.FieldAmount:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value != nil {
				rr.Amount = *value
			}
		case reconciliationrecord.FieldChannel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field channel", values[i])
			} else if value.Valid {
				rr.Channel = domain.OrderPaidChannel(value.String)
			}
		case reconciliationrecord.FieldDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				rr.Date = value.Time
			}
		default:
			rr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ReconciliationRecord.
// This includes values selected through modifiers, order, etc.
func (rr *ReconciliationRecord) Value(name string) (ent.Value, error) {
	return rr.selectValues.Get(name)
}

// Update returns a builder for updating this ReconciliationRecord.
// Note that you need to call ReconciliationRecord.Unwrap() before calling this method if this ReconciliationRecord
// was returned from a transaction, and the transaction was committed or rolled back.
func (rr *ReconciliationRecord) Update() *ReconciliationRecordUpdateOne {
	return NewReconciliationRecordClient(rr.config).UpdateOne(rr)
}

// Unwrap unwraps the ReconciliationRecord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rr *ReconciliationRecord) Unwrap() *ReconciliationRecord {
	_tx, ok := rr.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReconciliationRecord is not a transactional entity")
	}
	rr.config.driver = _tx.drv
	return rr
}

// String implements the fmt.Stringer.
func (rr *ReconciliationRecord) String() string {
	var builder strings.Builder
	builder.WriteString("ReconciliationRecord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rr.ID))
	builder.WriteString("created_at=")
	builder.WriteString(rr.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rr.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("no=")
	builder.WriteString(rr.No)
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", rr.StoreID))
	builder.WriteString(", ")
	builder.WriteString("store_name=")
	builder.WriteString(rr.StoreName)
	builder.WriteString(", ")
	builder.WriteString("order_count=")
	builder.WriteString(fmt.Sprintf("%v", rr.OrderCount))
	builder.WriteString(", ")
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", rr.Amount))
	builder.WriteString(", ")
	builder.WriteString("channel=")
	builder.WriteString(fmt.Sprintf("%v", rr.Channel))
	builder.WriteString(", ")
	builder.WriteString("date=")
	builder.WriteString(rr.Date.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ReconciliationRecords is a parsable slice of ReconciliationRecord.
type ReconciliationRecords []*ReconciliationRecord
