// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/refundorder"
	"gitlab.jiguang.dev/pos-dine/dine/ent/refundorderproduct"
)

// RefundOrderProductQuery is the builder for querying RefundOrderProduct entities.
type RefundOrderProductQuery struct {
	config
	ctx             *QueryContext
	order           []refundorderproduct.OrderOption
	inters          []Interceptor
	predicates      []predicate.RefundOrderProduct
	withRefundOrder *RefundOrderQuery
	modifiers       []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RefundOrderProductQuery builder.
func (ropq *RefundOrderProductQuery) Where(ps ...predicate.RefundOrderProduct) *RefundOrderProductQuery {
	ropq.predicates = append(ropq.predicates, ps...)
	return ropq
}

// Limit the number of records to be returned by this query.
func (ropq *RefundOrderProductQuery) Limit(limit int) *RefundOrderProductQuery {
	ropq.ctx.Limit = &limit
	return ropq
}

// Offset to start from.
func (ropq *RefundOrderProductQuery) Offset(offset int) *RefundOrderProductQuery {
	ropq.ctx.Offset = &offset
	return ropq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ropq *RefundOrderProductQuery) Unique(unique bool) *RefundOrderProductQuery {
	ropq.ctx.Unique = &unique
	return ropq
}

// Order specifies how the records should be ordered.
func (ropq *RefundOrderProductQuery) Order(o ...refundorderproduct.OrderOption) *RefundOrderProductQuery {
	ropq.order = append(ropq.order, o...)
	return ropq
}

// QueryRefundOrder chains the current query on the "refund_order" edge.
func (ropq *RefundOrderProductQuery) QueryRefundOrder() *RefundOrderQuery {
	query := (&RefundOrderClient{config: ropq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ropq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ropq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(refundorderproduct.Table, refundorderproduct.FieldID, selector),
			sqlgraph.To(refundorder.Table, refundorder.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, refundorderproduct.RefundOrderTable, refundorderproduct.RefundOrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(ropq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RefundOrderProduct entity from the query.
// Returns a *NotFoundError when no RefundOrderProduct was found.
func (ropq *RefundOrderProductQuery) First(ctx context.Context) (*RefundOrderProduct, error) {
	nodes, err := ropq.Limit(1).All(setContextOp(ctx, ropq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{refundorderproduct.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) FirstX(ctx context.Context) *RefundOrderProduct {
	node, err := ropq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RefundOrderProduct ID from the query.
// Returns a *NotFoundError when no RefundOrderProduct ID was found.
func (ropq *RefundOrderProductQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ropq.Limit(1).IDs(setContextOp(ctx, ropq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{refundorderproduct.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ropq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RefundOrderProduct entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RefundOrderProduct entity is found.
// Returns a *NotFoundError when no RefundOrderProduct entities are found.
func (ropq *RefundOrderProductQuery) Only(ctx context.Context) (*RefundOrderProduct, error) {
	nodes, err := ropq.Limit(2).All(setContextOp(ctx, ropq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{refundorderproduct.Label}
	default:
		return nil, &NotSingularError{refundorderproduct.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) OnlyX(ctx context.Context) *RefundOrderProduct {
	node, err := ropq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RefundOrderProduct ID in the query.
// Returns a *NotSingularError when more than one RefundOrderProduct ID is found.
// Returns a *NotFoundError when no entities are found.
func (ropq *RefundOrderProductQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ropq.Limit(2).IDs(setContextOp(ctx, ropq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{refundorderproduct.Label}
	default:
		err = &NotSingularError{refundorderproduct.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ropq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RefundOrderProducts.
func (ropq *RefundOrderProductQuery) All(ctx context.Context) ([]*RefundOrderProduct, error) {
	ctx = setContextOp(ctx, ropq.ctx, ent.OpQueryAll)
	if err := ropq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RefundOrderProduct, *RefundOrderProductQuery]()
	return withInterceptors[[]*RefundOrderProduct](ctx, ropq, qr, ropq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) AllX(ctx context.Context) []*RefundOrderProduct {
	nodes, err := ropq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RefundOrderProduct IDs.
func (ropq *RefundOrderProductQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ropq.ctx.Unique == nil && ropq.path != nil {
		ropq.Unique(true)
	}
	ctx = setContextOp(ctx, ropq.ctx, ent.OpQueryIDs)
	if err = ropq.Select(refundorderproduct.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ropq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ropq *RefundOrderProductQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ropq.ctx, ent.OpQueryCount)
	if err := ropq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ropq, querierCount[*RefundOrderProductQuery](), ropq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) CountX(ctx context.Context) int {
	count, err := ropq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ropq *RefundOrderProductQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ropq.ctx, ent.OpQueryExist)
	switch _, err := ropq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ropq *RefundOrderProductQuery) ExistX(ctx context.Context) bool {
	exist, err := ropq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RefundOrderProductQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ropq *RefundOrderProductQuery) Clone() *RefundOrderProductQuery {
	if ropq == nil {
		return nil
	}
	return &RefundOrderProductQuery{
		config:          ropq.config,
		ctx:             ropq.ctx.Clone(),
		order:           append([]refundorderproduct.OrderOption{}, ropq.order...),
		inters:          append([]Interceptor{}, ropq.inters...),
		predicates:      append([]predicate.RefundOrderProduct{}, ropq.predicates...),
		withRefundOrder: ropq.withRefundOrder.Clone(),
		// clone intermediate query.
		sql:       ropq.sql.Clone(),
		path:      ropq.path,
		modifiers: append([]func(*sql.Selector){}, ropq.modifiers...),
	}
}

// WithRefundOrder tells the query-builder to eager-load the nodes that are connected to
// the "refund_order" edge. The optional arguments are used to configure the query builder of the edge.
func (ropq *RefundOrderProductQuery) WithRefundOrder(opts ...func(*RefundOrderQuery)) *RefundOrderProductQuery {
	query := (&RefundOrderClient{config: ropq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ropq.withRefundOrder = query
	return ropq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RefundOrderProduct.Query().
//		GroupBy(refundorderproduct.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ropq *RefundOrderProductQuery) GroupBy(field string, fields ...string) *RefundOrderProductGroupBy {
	ropq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RefundOrderProductGroupBy{build: ropq}
	grbuild.flds = &ropq.ctx.Fields
	grbuild.label = refundorderproduct.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RefundOrderProduct.Query().
//		Select(refundorderproduct.FieldCreatedAt).
//		Scan(ctx, &v)
func (ropq *RefundOrderProductQuery) Select(fields ...string) *RefundOrderProductSelect {
	ropq.ctx.Fields = append(ropq.ctx.Fields, fields...)
	sbuild := &RefundOrderProductSelect{RefundOrderProductQuery: ropq}
	sbuild.label = refundorderproduct.Label
	sbuild.flds, sbuild.scan = &ropq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RefundOrderProductSelect configured with the given aggregations.
func (ropq *RefundOrderProductQuery) Aggregate(fns ...AggregateFunc) *RefundOrderProductSelect {
	return ropq.Select().Aggregate(fns...)
}

func (ropq *RefundOrderProductQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ropq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ropq); err != nil {
				return err
			}
		}
	}
	for _, f := range ropq.ctx.Fields {
		if !refundorderproduct.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ropq.path != nil {
		prev, err := ropq.path(ctx)
		if err != nil {
			return err
		}
		ropq.sql = prev
	}
	return nil
}

func (ropq *RefundOrderProductQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RefundOrderProduct, error) {
	var (
		nodes       = []*RefundOrderProduct{}
		_spec       = ropq.querySpec()
		loadedTypes = [1]bool{
			ropq.withRefundOrder != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RefundOrderProduct).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RefundOrderProduct{config: ropq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ropq.modifiers) > 0 {
		_spec.Modifiers = ropq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ropq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ropq.withRefundOrder; query != nil {
		if err := ropq.loadRefundOrder(ctx, query, nodes, nil,
			func(n *RefundOrderProduct, e *RefundOrder) { n.Edges.RefundOrder = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ropq *RefundOrderProductQuery) loadRefundOrder(ctx context.Context, query *RefundOrderQuery, nodes []*RefundOrderProduct, init func(*RefundOrderProduct), assign func(*RefundOrderProduct, *RefundOrder)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*RefundOrderProduct)
	for i := range nodes {
		fk := nodes[i].RefundOrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(refundorder.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "refund_order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ropq *RefundOrderProductQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ropq.querySpec()
	if len(ropq.modifiers) > 0 {
		_spec.Modifiers = ropq.modifiers
	}
	_spec.Node.Columns = ropq.ctx.Fields
	if len(ropq.ctx.Fields) > 0 {
		_spec.Unique = ropq.ctx.Unique != nil && *ropq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ropq.driver, _spec)
}

func (ropq *RefundOrderProductQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(refundorderproduct.Table, refundorderproduct.Columns, sqlgraph.NewFieldSpec(refundorderproduct.FieldID, field.TypeUUID))
	_spec.From = ropq.sql
	if unique := ropq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ropq.path != nil {
		_spec.Unique = true
	}
	if fields := ropq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, refundorderproduct.FieldID)
		for i := range fields {
			if fields[i] != refundorderproduct.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ropq.withRefundOrder != nil {
			_spec.Node.AddColumnOnce(refundorderproduct.FieldRefundOrderID)
		}
	}
	if ps := ropq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ropq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ropq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ropq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ropq *RefundOrderProductQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ropq.driver.Dialect())
	t1 := builder.Table(refundorderproduct.Table)
	columns := ropq.ctx.Fields
	if len(columns) == 0 {
		columns = refundorderproduct.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ropq.sql != nil {
		selector = ropq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ropq.ctx.Unique != nil && *ropq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ropq.modifiers {
		m(selector)
	}
	for _, p := range ropq.predicates {
		p(selector)
	}
	for _, p := range ropq.order {
		p(selector)
	}
	if offset := ropq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ropq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ropq *RefundOrderProductQuery) ForUpdate(opts ...sql.LockOption) *RefundOrderProductQuery {
	if ropq.driver.Dialect() == dialect.Postgres {
		ropq.Unique(false)
	}
	ropq.modifiers = append(ropq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ropq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ropq *RefundOrderProductQuery) ForShare(opts ...sql.LockOption) *RefundOrderProductQuery {
	if ropq.driver.Dialect() == dialect.Postgres {
		ropq.Unique(false)
	}
	ropq.modifiers = append(ropq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ropq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ropq *RefundOrderProductQuery) Modify(modifiers ...func(s *sql.Selector)) *RefundOrderProductSelect {
	ropq.modifiers = append(ropq.modifiers, modifiers...)
	return ropq.Select()
}

// RefundOrderProductGroupBy is the group-by builder for RefundOrderProduct entities.
type RefundOrderProductGroupBy struct {
	selector
	build *RefundOrderProductQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ropgb *RefundOrderProductGroupBy) Aggregate(fns ...AggregateFunc) *RefundOrderProductGroupBy {
	ropgb.fns = append(ropgb.fns, fns...)
	return ropgb
}

// Scan applies the selector query and scans the result into the given value.
func (ropgb *RefundOrderProductGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ropgb.build.ctx, ent.OpQueryGroupBy)
	if err := ropgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RefundOrderProductQuery, *RefundOrderProductGroupBy](ctx, ropgb.build, ropgb, ropgb.build.inters, v)
}

func (ropgb *RefundOrderProductGroupBy) sqlScan(ctx context.Context, root *RefundOrderProductQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ropgb.fns))
	for _, fn := range ropgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ropgb.flds)+len(ropgb.fns))
		for _, f := range *ropgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ropgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ropgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RefundOrderProductSelect is the builder for selecting fields of RefundOrderProduct entities.
type RefundOrderProductSelect struct {
	*RefundOrderProductQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rops *RefundOrderProductSelect) Aggregate(fns ...AggregateFunc) *RefundOrderProductSelect {
	rops.fns = append(rops.fns, fns...)
	return rops
}

// Scan applies the selector query and scans the result into the given value.
func (rops *RefundOrderProductSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rops.ctx, ent.OpQuerySelect)
	if err := rops.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RefundOrderProductQuery, *RefundOrderProductSelect](ctx, rops.RefundOrderProductQuery, rops, rops.inters, v)
}

func (rops *RefundOrderProductSelect) sqlScan(ctx context.Context, root *RefundOrderProductQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rops.fns))
	for _, fn := range rops.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rops.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rops.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rops *RefundOrderProductSelect) Modify(modifiers ...func(s *sql.Selector)) *RefundOrderProductSelect {
	rops.modifiers = append(rops.modifiers, modifiers...)
	return rops
}
