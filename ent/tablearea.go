// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"gitlab.jiguang.dev/pos-dine/dine/ent/tablearea"
)

// TableArea is the model entity for the TableArea schema.
type TableArea struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// StoreID holds the value of the "store_id" field.
	StoreID int `json:"store_id,omitempty"`
	// TableCount holds the value of the "table_count" field.
	TableCount int `json:"table_count,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TableAreaQuery when eager-loading is set.
	Edges        TableAreaEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TableAreaEdges holds the relations/edges for other nodes in the graph.
type TableAreaEdges struct {
	// Dinetables holds the value of the dinetables edge.
	Dinetables []*DineTable `json:"dinetables,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DinetablesOrErr returns the Dinetables value or an error if the edge
// was not loaded in eager-loading.
func (e TableAreaEdges) DinetablesOrErr() ([]*DineTable, error) {
	if e.loadedTypes[0] {
		return e.Dinetables, nil
	}
	return nil, &NotLoadedError{edge: "dinetables"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TableArea) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tablearea.FieldID, tablearea.FieldDeletedAt, tablearea.FieldStoreID, tablearea.FieldTableCount:
			values[i] = new(sql.NullInt64)
		case tablearea.FieldName:
			values[i] = new(sql.NullString)
		case tablearea.FieldCreatedAt, tablearea.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TableArea fields.
func (ta *TableArea) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tablearea.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ta.ID = int(value.Int64)
		case tablearea.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ta.CreatedAt = value.Time
			}
		case tablearea.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ta.UpdatedAt = value.Time
			}
		case tablearea.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				ta.DeletedAt = value.Int64
			}
		case tablearea.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				ta.Name = value.String
			}
		case tablearea.FieldStoreID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value.Valid {
				ta.StoreID = int(value.Int64)
			}
		case tablearea.FieldTableCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field table_count", values[i])
			} else if value.Valid {
				ta.TableCount = int(value.Int64)
			}
		default:
			ta.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TableArea.
// This includes values selected through modifiers, order, etc.
func (ta *TableArea) Value(name string) (ent.Value, error) {
	return ta.selectValues.Get(name)
}

// QueryDinetables queries the "dinetables" edge of the TableArea entity.
func (ta *TableArea) QueryDinetables() *DineTableQuery {
	return NewTableAreaClient(ta.config).QueryDinetables(ta)
}

// Update returns a builder for updating this TableArea.
// Note that you need to call TableArea.Unwrap() before calling this method if this TableArea
// was returned from a transaction, and the transaction was committed or rolled back.
func (ta *TableArea) Update() *TableAreaUpdateOne {
	return NewTableAreaClient(ta.config).UpdateOne(ta)
}

// Unwrap unwraps the TableArea entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ta *TableArea) Unwrap() *TableArea {
	_tx, ok := ta.config.driver.(*txDriver)
	if !ok {
		panic("ent: TableArea is not a transactional entity")
	}
	ta.config.driver = _tx.drv
	return ta
}

// String implements the fmt.Stringer.
func (ta *TableArea) String() string {
	var builder strings.Builder
	builder.WriteString("TableArea(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ta.ID))
	builder.WriteString("created_at=")
	builder.WriteString(ta.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ta.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", ta.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(ta.Name)
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", ta.StoreID))
	builder.WriteString(", ")
	builder.WriteString("table_count=")
	builder.WriteString(fmt.Sprintf("%v", ta.TableCount))
	builder.WriteByte(')')
	return builder.String()
}

// TableAreas is a parsable slice of TableArea.
type TableAreas []*TableArea
