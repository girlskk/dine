// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.jiguang.dev/pos-dine/dine/ent/orderfinancelog"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
)

// OrderFinanceLogQuery is the builder for querying OrderFinanceLog entities.
type OrderFinanceLogQuery struct {
	config
	ctx        *QueryContext
	order      []orderfinancelog.OrderOption
	inters     []Interceptor
	predicates []predicate.OrderFinanceLog
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderFinanceLogQuery builder.
func (oflq *OrderFinanceLogQuery) Where(ps ...predicate.OrderFinanceLog) *OrderFinanceLogQuery {
	oflq.predicates = append(oflq.predicates, ps...)
	return oflq
}

// Limit the number of records to be returned by this query.
func (oflq *OrderFinanceLogQuery) Limit(limit int) *OrderFinanceLogQuery {
	oflq.ctx.Limit = &limit
	return oflq
}

// Offset to start from.
func (oflq *OrderFinanceLogQuery) Offset(offset int) *OrderFinanceLogQuery {
	oflq.ctx.Offset = &offset
	return oflq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oflq *OrderFinanceLogQuery) Unique(unique bool) *OrderFinanceLogQuery {
	oflq.ctx.Unique = &unique
	return oflq
}

// Order specifies how the records should be ordered.
func (oflq *OrderFinanceLogQuery) Order(o ...orderfinancelog.OrderOption) *OrderFinanceLogQuery {
	oflq.order = append(oflq.order, o...)
	return oflq
}

// First returns the first OrderFinanceLog entity from the query.
// Returns a *NotFoundError when no OrderFinanceLog was found.
func (oflq *OrderFinanceLogQuery) First(ctx context.Context) (*OrderFinanceLog, error) {
	nodes, err := oflq.Limit(1).All(setContextOp(ctx, oflq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderfinancelog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) FirstX(ctx context.Context) *OrderFinanceLog {
	node, err := oflq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderFinanceLog ID from the query.
// Returns a *NotFoundError when no OrderFinanceLog ID was found.
func (oflq *OrderFinanceLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oflq.Limit(1).IDs(setContextOp(ctx, oflq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderfinancelog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) FirstIDX(ctx context.Context) int {
	id, err := oflq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderFinanceLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderFinanceLog entity is found.
// Returns a *NotFoundError when no OrderFinanceLog entities are found.
func (oflq *OrderFinanceLogQuery) Only(ctx context.Context) (*OrderFinanceLog, error) {
	nodes, err := oflq.Limit(2).All(setContextOp(ctx, oflq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderfinancelog.Label}
	default:
		return nil, &NotSingularError{orderfinancelog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) OnlyX(ctx context.Context) *OrderFinanceLog {
	node, err := oflq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderFinanceLog ID in the query.
// Returns a *NotSingularError when more than one OrderFinanceLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (oflq *OrderFinanceLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = oflq.Limit(2).IDs(setContextOp(ctx, oflq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderfinancelog.Label}
	default:
		err = &NotSingularError{orderfinancelog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := oflq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderFinanceLogs.
func (oflq *OrderFinanceLogQuery) All(ctx context.Context) ([]*OrderFinanceLog, error) {
	ctx = setContextOp(ctx, oflq.ctx, ent.OpQueryAll)
	if err := oflq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrderFinanceLog, *OrderFinanceLogQuery]()
	return withInterceptors[[]*OrderFinanceLog](ctx, oflq, qr, oflq.inters)
}

// AllX is like All, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) AllX(ctx context.Context) []*OrderFinanceLog {
	nodes, err := oflq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderFinanceLog IDs.
func (oflq *OrderFinanceLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if oflq.ctx.Unique == nil && oflq.path != nil {
		oflq.Unique(true)
	}
	ctx = setContextOp(ctx, oflq.ctx, ent.OpQueryIDs)
	if err = oflq.Select(orderfinancelog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) IDsX(ctx context.Context) []int {
	ids, err := oflq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oflq *OrderFinanceLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, oflq.ctx, ent.OpQueryCount)
	if err := oflq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, oflq, querierCount[*OrderFinanceLogQuery](), oflq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) CountX(ctx context.Context) int {
	count, err := oflq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oflq *OrderFinanceLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, oflq.ctx, ent.OpQueryExist)
	switch _, err := oflq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (oflq *OrderFinanceLogQuery) ExistX(ctx context.Context) bool {
	exist, err := oflq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderFinanceLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oflq *OrderFinanceLogQuery) Clone() *OrderFinanceLogQuery {
	if oflq == nil {
		return nil
	}
	return &OrderFinanceLogQuery{
		config:     oflq.config,
		ctx:        oflq.ctx.Clone(),
		order:      append([]orderfinancelog.OrderOption{}, oflq.order...),
		inters:     append([]Interceptor{}, oflq.inters...),
		predicates: append([]predicate.OrderFinanceLog{}, oflq.predicates...),
		// clone intermediate query.
		sql:       oflq.sql.Clone(),
		path:      oflq.path,
		modifiers: append([]func(*sql.Selector){}, oflq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderFinanceLog.Query().
//		GroupBy(orderfinancelog.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (oflq *OrderFinanceLogQuery) GroupBy(field string, fields ...string) *OrderFinanceLogGroupBy {
	oflq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrderFinanceLogGroupBy{build: oflq}
	grbuild.flds = &oflq.ctx.Fields
	grbuild.label = orderfinancelog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.OrderFinanceLog.Query().
//		Select(orderfinancelog.FieldCreatedAt).
//		Scan(ctx, &v)
func (oflq *OrderFinanceLogQuery) Select(fields ...string) *OrderFinanceLogSelect {
	oflq.ctx.Fields = append(oflq.ctx.Fields, fields...)
	sbuild := &OrderFinanceLogSelect{OrderFinanceLogQuery: oflq}
	sbuild.label = orderfinancelog.Label
	sbuild.flds, sbuild.scan = &oflq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrderFinanceLogSelect configured with the given aggregations.
func (oflq *OrderFinanceLogQuery) Aggregate(fns ...AggregateFunc) *OrderFinanceLogSelect {
	return oflq.Select().Aggregate(fns...)
}

func (oflq *OrderFinanceLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range oflq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, oflq); err != nil {
				return err
			}
		}
	}
	for _, f := range oflq.ctx.Fields {
		if !orderfinancelog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if oflq.path != nil {
		prev, err := oflq.path(ctx)
		if err != nil {
			return err
		}
		oflq.sql = prev
	}
	return nil
}

func (oflq *OrderFinanceLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderFinanceLog, error) {
	var (
		nodes = []*OrderFinanceLog{}
		_spec = oflq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrderFinanceLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrderFinanceLog{config: oflq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(oflq.modifiers) > 0 {
		_spec.Modifiers = oflq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oflq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (oflq *OrderFinanceLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oflq.querySpec()
	if len(oflq.modifiers) > 0 {
		_spec.Modifiers = oflq.modifiers
	}
	_spec.Node.Columns = oflq.ctx.Fields
	if len(oflq.ctx.Fields) > 0 {
		_spec.Unique = oflq.ctx.Unique != nil && *oflq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, oflq.driver, _spec)
}

func (oflq *OrderFinanceLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(orderfinancelog.Table, orderfinancelog.Columns, sqlgraph.NewFieldSpec(orderfinancelog.FieldID, field.TypeInt))
	_spec.From = oflq.sql
	if unique := oflq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if oflq.path != nil {
		_spec.Unique = true
	}
	if fields := oflq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderfinancelog.FieldID)
		for i := range fields {
			if fields[i] != orderfinancelog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := oflq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oflq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oflq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oflq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oflq *OrderFinanceLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oflq.driver.Dialect())
	t1 := builder.Table(orderfinancelog.Table)
	columns := oflq.ctx.Fields
	if len(columns) == 0 {
		columns = orderfinancelog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oflq.sql != nil {
		selector = oflq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oflq.ctx.Unique != nil && *oflq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range oflq.modifiers {
		m(selector)
	}
	for _, p := range oflq.predicates {
		p(selector)
	}
	for _, p := range oflq.order {
		p(selector)
	}
	if offset := oflq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oflq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (oflq *OrderFinanceLogQuery) ForUpdate(opts ...sql.LockOption) *OrderFinanceLogQuery {
	if oflq.driver.Dialect() == dialect.Postgres {
		oflq.Unique(false)
	}
	oflq.modifiers = append(oflq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return oflq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (oflq *OrderFinanceLogQuery) ForShare(opts ...sql.LockOption) *OrderFinanceLogQuery {
	if oflq.driver.Dialect() == dialect.Postgres {
		oflq.Unique(false)
	}
	oflq.modifiers = append(oflq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return oflq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (oflq *OrderFinanceLogQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderFinanceLogSelect {
	oflq.modifiers = append(oflq.modifiers, modifiers...)
	return oflq.Select()
}

// OrderFinanceLogGroupBy is the group-by builder for OrderFinanceLog entities.
type OrderFinanceLogGroupBy struct {
	selector
	build *OrderFinanceLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (oflgb *OrderFinanceLogGroupBy) Aggregate(fns ...AggregateFunc) *OrderFinanceLogGroupBy {
	oflgb.fns = append(oflgb.fns, fns...)
	return oflgb
}

// Scan applies the selector query and scans the result into the given value.
func (oflgb *OrderFinanceLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oflgb.build.ctx, ent.OpQueryGroupBy)
	if err := oflgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderFinanceLogQuery, *OrderFinanceLogGroupBy](ctx, oflgb.build, oflgb, oflgb.build.inters, v)
}

func (oflgb *OrderFinanceLogGroupBy) sqlScan(ctx context.Context, root *OrderFinanceLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(oflgb.fns))
	for _, fn := range oflgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*oflgb.flds)+len(oflgb.fns))
		for _, f := range *oflgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*oflgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oflgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrderFinanceLogSelect is the builder for selecting fields of OrderFinanceLog entities.
type OrderFinanceLogSelect struct {
	*OrderFinanceLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ofls *OrderFinanceLogSelect) Aggregate(fns ...AggregateFunc) *OrderFinanceLogSelect {
	ofls.fns = append(ofls.fns, fns...)
	return ofls
}

// Scan applies the selector query and scans the result into the given value.
func (ofls *OrderFinanceLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ofls.ctx, ent.OpQuerySelect)
	if err := ofls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderFinanceLogQuery, *OrderFinanceLogSelect](ctx, ofls.OrderFinanceLogQuery, ofls, ofls.inters, v)
}

func (ofls *OrderFinanceLogSelect) sqlScan(ctx context.Context, root *OrderFinanceLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ofls.fns))
	for _, fn := range ofls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ofls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ofls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ofls *OrderFinanceLogSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderFinanceLogSelect {
	ofls.modifiers = append(ofls.modifiers, modifiers...)
	return ofls
}
