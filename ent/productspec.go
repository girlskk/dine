// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"gitlab.jiguang.dev/pos-dine/dine/ent/product"
	"gitlab.jiguang.dev/pos-dine/dine/ent/productspec"
	"gitlab.jiguang.dev/pos-dine/dine/ent/spec"
)

// ProductSpec is the model entity for the ProductSpec schema.
type ProductSpec struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 规格价格
	Price decimal.Decimal `json:"price,omitempty"`
	// 商品可售状态
	SaleStatus int `json:"sale_status,omitempty"`
	// ProductID holds the value of the "product_id" field.
	ProductID int `json:"product_id,omitempty"`
	// SpecID holds the value of the "spec_id" field.
	SpecID int `json:"spec_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProductSpecQuery when eager-loading is set.
	Edges        ProductSpecEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProductSpecEdges holds the relations/edges for other nodes in the graph.
type ProductSpecEdges struct {
	// Product holds the value of the product edge.
	Product *Product `json:"product,omitempty"`
	// Spec holds the value of the spec edge.
	Spec *Spec `json:"spec,omitempty"`
	// SetMealDetails holds the value of the set_meal_details edge.
	SetMealDetails []*SetMealDetail `json:"set_meal_details,omitempty"`
	// OrderCartItems holds the value of the order_cart_items edge.
	OrderCartItems []*OrderCart `json:"order_cart_items,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// ProductOrErr returns the Product value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProductSpecEdges) ProductOrErr() (*Product, error) {
	if e.Product != nil {
		return e.Product, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: product.Label}
	}
	return nil, &NotLoadedError{edge: "product"}
}

// SpecOrErr returns the Spec value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProductSpecEdges) SpecOrErr() (*Spec, error) {
	if e.Spec != nil {
		return e.Spec, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: spec.Label}
	}
	return nil, &NotLoadedError{edge: "spec"}
}

// SetMealDetailsOrErr returns the SetMealDetails value or an error if the edge
// was not loaded in eager-loading.
func (e ProductSpecEdges) SetMealDetailsOrErr() ([]*SetMealDetail, error) {
	if e.loadedTypes[2] {
		return e.SetMealDetails, nil
	}
	return nil, &NotLoadedError{edge: "set_meal_details"}
}

// OrderCartItemsOrErr returns the OrderCartItems value or an error if the edge
// was not loaded in eager-loading.
func (e ProductSpecEdges) OrderCartItemsOrErr() ([]*OrderCart, error) {
	if e.loadedTypes[3] {
		return e.OrderCartItems, nil
	}
	return nil, &NotLoadedError{edge: "order_cart_items"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductSpec) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case productspec.FieldPrice:
			values[i] = new(decimal.Decimal)
		case productspec.FieldID, productspec.FieldSaleStatus, productspec.FieldProductID, productspec.FieldSpecID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductSpec fields.
func (ps *ProductSpec) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productspec.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ps.ID = int(value.Int64)
		case productspec.FieldPrice:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value != nil {
				ps.Price = *value
			}
		case productspec.FieldSaleStatus:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sale_status", values[i])
			} else if value.Valid {
				ps.SaleStatus = int(value.Int64)
			}
		case productspec.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				ps.ProductID = int(value.Int64)
			}
		case productspec.FieldSpecID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field spec_id", values[i])
			} else if value.Valid {
				ps.SpecID = int(value.Int64)
			}
		default:
			ps.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductSpec.
// This includes values selected through modifiers, order, etc.
func (ps *ProductSpec) Value(name string) (ent.Value, error) {
	return ps.selectValues.Get(name)
}

// QueryProduct queries the "product" edge of the ProductSpec entity.
func (ps *ProductSpec) QueryProduct() *ProductQuery {
	return NewProductSpecClient(ps.config).QueryProduct(ps)
}

// QuerySpec queries the "spec" edge of the ProductSpec entity.
func (ps *ProductSpec) QuerySpec() *SpecQuery {
	return NewProductSpecClient(ps.config).QuerySpec(ps)
}

// QuerySetMealDetails queries the "set_meal_details" edge of the ProductSpec entity.
func (ps *ProductSpec) QuerySetMealDetails() *SetMealDetailQuery {
	return NewProductSpecClient(ps.config).QuerySetMealDetails(ps)
}

// QueryOrderCartItems queries the "order_cart_items" edge of the ProductSpec entity.
func (ps *ProductSpec) QueryOrderCartItems() *OrderCartQuery {
	return NewProductSpecClient(ps.config).QueryOrderCartItems(ps)
}

// Update returns a builder for updating this ProductSpec.
// Note that you need to call ProductSpec.Unwrap() before calling this method if this ProductSpec
// was returned from a transaction, and the transaction was committed or rolled back.
func (ps *ProductSpec) Update() *ProductSpecUpdateOne {
	return NewProductSpecClient(ps.config).UpdateOne(ps)
}

// Unwrap unwraps the ProductSpec entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ps *ProductSpec) Unwrap() *ProductSpec {
	_tx, ok := ps.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductSpec is not a transactional entity")
	}
	ps.config.driver = _tx.drv
	return ps
}

// String implements the fmt.Stringer.
func (ps *ProductSpec) String() string {
	var builder strings.Builder
	builder.WriteString("ProductSpec(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ps.ID))
	builder.WriteString("price=")
	builder.WriteString(fmt.Sprintf("%v", ps.Price))
	builder.WriteString(", ")
	builder.WriteString("sale_status=")
	builder.WriteString(fmt.Sprintf("%v", ps.SaleStatus))
	builder.WriteString(", ")
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", ps.ProductID))
	builder.WriteString(", ")
	builder.WriteString("spec_id=")
	builder.WriteString(fmt.Sprintf("%v", ps.SpecID))
	builder.WriteByte(')')
	return builder.String()
}

// ProductSpecs is a parsable slice of ProductSpec.
type ProductSpecs []*ProductSpec
