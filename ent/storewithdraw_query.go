// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
	"gitlab.jiguang.dev/pos-dine/dine/ent/storewithdraw"
)

// StoreWithdrawQuery is the builder for querying StoreWithdraw entities.
type StoreWithdrawQuery struct {
	config
	ctx        *QueryContext
	order      []storewithdraw.OrderOption
	inters     []Interceptor
	predicates []predicate.StoreWithdraw
	withStore  *StoreQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StoreWithdrawQuery builder.
func (swq *StoreWithdrawQuery) Where(ps ...predicate.StoreWithdraw) *StoreWithdrawQuery {
	swq.predicates = append(swq.predicates, ps...)
	return swq
}

// Limit the number of records to be returned by this query.
func (swq *StoreWithdrawQuery) Limit(limit int) *StoreWithdrawQuery {
	swq.ctx.Limit = &limit
	return swq
}

// Offset to start from.
func (swq *StoreWithdrawQuery) Offset(offset int) *StoreWithdrawQuery {
	swq.ctx.Offset = &offset
	return swq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (swq *StoreWithdrawQuery) Unique(unique bool) *StoreWithdrawQuery {
	swq.ctx.Unique = &unique
	return swq
}

// Order specifies how the records should be ordered.
func (swq *StoreWithdrawQuery) Order(o ...storewithdraw.OrderOption) *StoreWithdrawQuery {
	swq.order = append(swq.order, o...)
	return swq
}

// QueryStore chains the current query on the "store" edge.
func (swq *StoreWithdrawQuery) QueryStore() *StoreQuery {
	query := (&StoreClient{config: swq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := swq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := swq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(storewithdraw.Table, storewithdraw.FieldID, selector),
			sqlgraph.To(store.Table, store.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, storewithdraw.StoreTable, storewithdraw.StoreColumn),
		)
		fromU = sqlgraph.SetNeighbors(swq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StoreWithdraw entity from the query.
// Returns a *NotFoundError when no StoreWithdraw was found.
func (swq *StoreWithdrawQuery) First(ctx context.Context) (*StoreWithdraw, error) {
	nodes, err := swq.Limit(1).All(setContextOp(ctx, swq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{storewithdraw.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (swq *StoreWithdrawQuery) FirstX(ctx context.Context) *StoreWithdraw {
	node, err := swq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StoreWithdraw ID from the query.
// Returns a *NotFoundError when no StoreWithdraw ID was found.
func (swq *StoreWithdrawQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = swq.Limit(1).IDs(setContextOp(ctx, swq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{storewithdraw.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (swq *StoreWithdrawQuery) FirstIDX(ctx context.Context) int {
	id, err := swq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StoreWithdraw entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StoreWithdraw entity is found.
// Returns a *NotFoundError when no StoreWithdraw entities are found.
func (swq *StoreWithdrawQuery) Only(ctx context.Context) (*StoreWithdraw, error) {
	nodes, err := swq.Limit(2).All(setContextOp(ctx, swq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{storewithdraw.Label}
	default:
		return nil, &NotSingularError{storewithdraw.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (swq *StoreWithdrawQuery) OnlyX(ctx context.Context) *StoreWithdraw {
	node, err := swq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StoreWithdraw ID in the query.
// Returns a *NotSingularError when more than one StoreWithdraw ID is found.
// Returns a *NotFoundError when no entities are found.
func (swq *StoreWithdrawQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = swq.Limit(2).IDs(setContextOp(ctx, swq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{storewithdraw.Label}
	default:
		err = &NotSingularError{storewithdraw.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (swq *StoreWithdrawQuery) OnlyIDX(ctx context.Context) int {
	id, err := swq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StoreWithdraws.
func (swq *StoreWithdrawQuery) All(ctx context.Context) ([]*StoreWithdraw, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryAll)
	if err := swq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StoreWithdraw, *StoreWithdrawQuery]()
	return withInterceptors[[]*StoreWithdraw](ctx, swq, qr, swq.inters)
}

// AllX is like All, but panics if an error occurs.
func (swq *StoreWithdrawQuery) AllX(ctx context.Context) []*StoreWithdraw {
	nodes, err := swq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StoreWithdraw IDs.
func (swq *StoreWithdrawQuery) IDs(ctx context.Context) (ids []int, err error) {
	if swq.ctx.Unique == nil && swq.path != nil {
		swq.Unique(true)
	}
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryIDs)
	if err = swq.Select(storewithdraw.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (swq *StoreWithdrawQuery) IDsX(ctx context.Context) []int {
	ids, err := swq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (swq *StoreWithdrawQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryCount)
	if err := swq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, swq, querierCount[*StoreWithdrawQuery](), swq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (swq *StoreWithdrawQuery) CountX(ctx context.Context) int {
	count, err := swq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (swq *StoreWithdrawQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, swq.ctx, ent.OpQueryExist)
	switch _, err := swq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (swq *StoreWithdrawQuery) ExistX(ctx context.Context) bool {
	exist, err := swq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StoreWithdrawQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (swq *StoreWithdrawQuery) Clone() *StoreWithdrawQuery {
	if swq == nil {
		return nil
	}
	return &StoreWithdrawQuery{
		config:     swq.config,
		ctx:        swq.ctx.Clone(),
		order:      append([]storewithdraw.OrderOption{}, swq.order...),
		inters:     append([]Interceptor{}, swq.inters...),
		predicates: append([]predicate.StoreWithdraw{}, swq.predicates...),
		withStore:  swq.withStore.Clone(),
		// clone intermediate query.
		sql:       swq.sql.Clone(),
		path:      swq.path,
		modifiers: append([]func(*sql.Selector){}, swq.modifiers...),
	}
}

// WithStore tells the query-builder to eager-load the nodes that are connected to
// the "store" edge. The optional arguments are used to configure the query builder of the edge.
func (swq *StoreWithdrawQuery) WithStore(opts ...func(*StoreQuery)) *StoreWithdrawQuery {
	query := (&StoreClient{config: swq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	swq.withStore = query
	return swq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StoreWithdraw.Query().
//		GroupBy(storewithdraw.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (swq *StoreWithdrawQuery) GroupBy(field string, fields ...string) *StoreWithdrawGroupBy {
	swq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StoreWithdrawGroupBy{build: swq}
	grbuild.flds = &swq.ctx.Fields
	grbuild.label = storewithdraw.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.StoreWithdraw.Query().
//		Select(storewithdraw.FieldCreatedAt).
//		Scan(ctx, &v)
func (swq *StoreWithdrawQuery) Select(fields ...string) *StoreWithdrawSelect {
	swq.ctx.Fields = append(swq.ctx.Fields, fields...)
	sbuild := &StoreWithdrawSelect{StoreWithdrawQuery: swq}
	sbuild.label = storewithdraw.Label
	sbuild.flds, sbuild.scan = &swq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StoreWithdrawSelect configured with the given aggregations.
func (swq *StoreWithdrawQuery) Aggregate(fns ...AggregateFunc) *StoreWithdrawSelect {
	return swq.Select().Aggregate(fns...)
}

func (swq *StoreWithdrawQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range swq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, swq); err != nil {
				return err
			}
		}
	}
	for _, f := range swq.ctx.Fields {
		if !storewithdraw.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if swq.path != nil {
		prev, err := swq.path(ctx)
		if err != nil {
			return err
		}
		swq.sql = prev
	}
	return nil
}

func (swq *StoreWithdrawQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StoreWithdraw, error) {
	var (
		nodes       = []*StoreWithdraw{}
		_spec       = swq.querySpec()
		loadedTypes = [1]bool{
			swq.withStore != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StoreWithdraw).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StoreWithdraw{config: swq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(swq.modifiers) > 0 {
		_spec.Modifiers = swq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, swq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := swq.withStore; query != nil {
		if err := swq.loadStore(ctx, query, nodes, nil,
			func(n *StoreWithdraw, e *Store) { n.Edges.Store = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (swq *StoreWithdrawQuery) loadStore(ctx context.Context, query *StoreQuery, nodes []*StoreWithdraw, init func(*StoreWithdraw), assign func(*StoreWithdraw, *Store)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*StoreWithdraw)
	for i := range nodes {
		fk := nodes[i].StoreID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(store.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "store_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (swq *StoreWithdrawQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := swq.querySpec()
	if len(swq.modifiers) > 0 {
		_spec.Modifiers = swq.modifiers
	}
	_spec.Node.Columns = swq.ctx.Fields
	if len(swq.ctx.Fields) > 0 {
		_spec.Unique = swq.ctx.Unique != nil && *swq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, swq.driver, _spec)
}

func (swq *StoreWithdrawQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(storewithdraw.Table, storewithdraw.Columns, sqlgraph.NewFieldSpec(storewithdraw.FieldID, field.TypeInt))
	_spec.From = swq.sql
	if unique := swq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if swq.path != nil {
		_spec.Unique = true
	}
	if fields := swq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, storewithdraw.FieldID)
		for i := range fields {
			if fields[i] != storewithdraw.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if swq.withStore != nil {
			_spec.Node.AddColumnOnce(storewithdraw.FieldStoreID)
		}
	}
	if ps := swq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := swq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := swq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := swq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (swq *StoreWithdrawQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(swq.driver.Dialect())
	t1 := builder.Table(storewithdraw.Table)
	columns := swq.ctx.Fields
	if len(columns) == 0 {
		columns = storewithdraw.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if swq.sql != nil {
		selector = swq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if swq.ctx.Unique != nil && *swq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range swq.modifiers {
		m(selector)
	}
	for _, p := range swq.predicates {
		p(selector)
	}
	for _, p := range swq.order {
		p(selector)
	}
	if offset := swq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := swq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (swq *StoreWithdrawQuery) ForUpdate(opts ...sql.LockOption) *StoreWithdrawQuery {
	if swq.driver.Dialect() == dialect.Postgres {
		swq.Unique(false)
	}
	swq.modifiers = append(swq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return swq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (swq *StoreWithdrawQuery) ForShare(opts ...sql.LockOption) *StoreWithdrawQuery {
	if swq.driver.Dialect() == dialect.Postgres {
		swq.Unique(false)
	}
	swq.modifiers = append(swq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return swq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (swq *StoreWithdrawQuery) Modify(modifiers ...func(s *sql.Selector)) *StoreWithdrawSelect {
	swq.modifiers = append(swq.modifiers, modifiers...)
	return swq.Select()
}

// StoreWithdrawGroupBy is the group-by builder for StoreWithdraw entities.
type StoreWithdrawGroupBy struct {
	selector
	build *StoreWithdrawQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (swgb *StoreWithdrawGroupBy) Aggregate(fns ...AggregateFunc) *StoreWithdrawGroupBy {
	swgb.fns = append(swgb.fns, fns...)
	return swgb
}

// Scan applies the selector query and scans the result into the given value.
func (swgb *StoreWithdrawGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, swgb.build.ctx, ent.OpQueryGroupBy)
	if err := swgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StoreWithdrawQuery, *StoreWithdrawGroupBy](ctx, swgb.build, swgb, swgb.build.inters, v)
}

func (swgb *StoreWithdrawGroupBy) sqlScan(ctx context.Context, root *StoreWithdrawQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(swgb.fns))
	for _, fn := range swgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*swgb.flds)+len(swgb.fns))
		for _, f := range *swgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*swgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := swgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StoreWithdrawSelect is the builder for selecting fields of StoreWithdraw entities.
type StoreWithdrawSelect struct {
	*StoreWithdrawQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sws *StoreWithdrawSelect) Aggregate(fns ...AggregateFunc) *StoreWithdrawSelect {
	sws.fns = append(sws.fns, fns...)
	return sws
}

// Scan applies the selector query and scans the result into the given value.
func (sws *StoreWithdrawSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sws.ctx, ent.OpQuerySelect)
	if err := sws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StoreWithdrawQuery, *StoreWithdrawSelect](ctx, sws.StoreWithdrawQuery, sws, sws.inters, v)
}

func (sws *StoreWithdrawSelect) sqlScan(ctx context.Context, root *StoreWithdrawQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sws.fns))
	for _, fn := range sws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sws *StoreWithdrawSelect) Modify(modifiers ...func(s *sql.Selector)) *StoreWithdrawSelect {
	sws.modifiers = append(sws.modifiers, modifiers...)
	return sws
}
