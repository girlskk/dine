// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
	"gitlab.jiguang.dev/pos-dine/dine/ent/storeaccounttransaction"
)

// StoreAccountTransactionQuery is the builder for querying StoreAccountTransaction entities.
type StoreAccountTransactionQuery struct {
	config
	ctx        *QueryContext
	order      []storeaccounttransaction.OrderOption
	inters     []Interceptor
	predicates []predicate.StoreAccountTransaction
	withStore  *StoreQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StoreAccountTransactionQuery builder.
func (satq *StoreAccountTransactionQuery) Where(ps ...predicate.StoreAccountTransaction) *StoreAccountTransactionQuery {
	satq.predicates = append(satq.predicates, ps...)
	return satq
}

// Limit the number of records to be returned by this query.
func (satq *StoreAccountTransactionQuery) Limit(limit int) *StoreAccountTransactionQuery {
	satq.ctx.Limit = &limit
	return satq
}

// Offset to start from.
func (satq *StoreAccountTransactionQuery) Offset(offset int) *StoreAccountTransactionQuery {
	satq.ctx.Offset = &offset
	return satq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (satq *StoreAccountTransactionQuery) Unique(unique bool) *StoreAccountTransactionQuery {
	satq.ctx.Unique = &unique
	return satq
}

// Order specifies how the records should be ordered.
func (satq *StoreAccountTransactionQuery) Order(o ...storeaccounttransaction.OrderOption) *StoreAccountTransactionQuery {
	satq.order = append(satq.order, o...)
	return satq
}

// QueryStore chains the current query on the "store" edge.
func (satq *StoreAccountTransactionQuery) QueryStore() *StoreQuery {
	query := (&StoreClient{config: satq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := satq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := satq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(storeaccounttransaction.Table, storeaccounttransaction.FieldID, selector),
			sqlgraph.To(store.Table, store.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, storeaccounttransaction.StoreTable, storeaccounttransaction.StoreColumn),
		)
		fromU = sqlgraph.SetNeighbors(satq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StoreAccountTransaction entity from the query.
// Returns a *NotFoundError when no StoreAccountTransaction was found.
func (satq *StoreAccountTransactionQuery) First(ctx context.Context) (*StoreAccountTransaction, error) {
	nodes, err := satq.Limit(1).All(setContextOp(ctx, satq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{storeaccounttransaction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) FirstX(ctx context.Context) *StoreAccountTransaction {
	node, err := satq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StoreAccountTransaction ID from the query.
// Returns a *NotFoundError when no StoreAccountTransaction ID was found.
func (satq *StoreAccountTransactionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = satq.Limit(1).IDs(setContextOp(ctx, satq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{storeaccounttransaction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) FirstIDX(ctx context.Context) int {
	id, err := satq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StoreAccountTransaction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StoreAccountTransaction entity is found.
// Returns a *NotFoundError when no StoreAccountTransaction entities are found.
func (satq *StoreAccountTransactionQuery) Only(ctx context.Context) (*StoreAccountTransaction, error) {
	nodes, err := satq.Limit(2).All(setContextOp(ctx, satq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{storeaccounttransaction.Label}
	default:
		return nil, &NotSingularError{storeaccounttransaction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) OnlyX(ctx context.Context) *StoreAccountTransaction {
	node, err := satq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StoreAccountTransaction ID in the query.
// Returns a *NotSingularError when more than one StoreAccountTransaction ID is found.
// Returns a *NotFoundError when no entities are found.
func (satq *StoreAccountTransactionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = satq.Limit(2).IDs(setContextOp(ctx, satq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{storeaccounttransaction.Label}
	default:
		err = &NotSingularError{storeaccounttransaction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) OnlyIDX(ctx context.Context) int {
	id, err := satq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StoreAccountTransactions.
func (satq *StoreAccountTransactionQuery) All(ctx context.Context) ([]*StoreAccountTransaction, error) {
	ctx = setContextOp(ctx, satq.ctx, ent.OpQueryAll)
	if err := satq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StoreAccountTransaction, *StoreAccountTransactionQuery]()
	return withInterceptors[[]*StoreAccountTransaction](ctx, satq, qr, satq.inters)
}

// AllX is like All, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) AllX(ctx context.Context) []*StoreAccountTransaction {
	nodes, err := satq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StoreAccountTransaction IDs.
func (satq *StoreAccountTransactionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if satq.ctx.Unique == nil && satq.path != nil {
		satq.Unique(true)
	}
	ctx = setContextOp(ctx, satq.ctx, ent.OpQueryIDs)
	if err = satq.Select(storeaccounttransaction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) IDsX(ctx context.Context) []int {
	ids, err := satq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (satq *StoreAccountTransactionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, satq.ctx, ent.OpQueryCount)
	if err := satq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, satq, querierCount[*StoreAccountTransactionQuery](), satq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) CountX(ctx context.Context) int {
	count, err := satq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (satq *StoreAccountTransactionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, satq.ctx, ent.OpQueryExist)
	switch _, err := satq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (satq *StoreAccountTransactionQuery) ExistX(ctx context.Context) bool {
	exist, err := satq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StoreAccountTransactionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (satq *StoreAccountTransactionQuery) Clone() *StoreAccountTransactionQuery {
	if satq == nil {
		return nil
	}
	return &StoreAccountTransactionQuery{
		config:     satq.config,
		ctx:        satq.ctx.Clone(),
		order:      append([]storeaccounttransaction.OrderOption{}, satq.order...),
		inters:     append([]Interceptor{}, satq.inters...),
		predicates: append([]predicate.StoreAccountTransaction{}, satq.predicates...),
		withStore:  satq.withStore.Clone(),
		// clone intermediate query.
		sql:       satq.sql.Clone(),
		path:      satq.path,
		modifiers: append([]func(*sql.Selector){}, satq.modifiers...),
	}
}

// WithStore tells the query-builder to eager-load the nodes that are connected to
// the "store" edge. The optional arguments are used to configure the query builder of the edge.
func (satq *StoreAccountTransactionQuery) WithStore(opts ...func(*StoreQuery)) *StoreAccountTransactionQuery {
	query := (&StoreClient{config: satq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	satq.withStore = query
	return satq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StoreAccountTransaction.Query().
//		GroupBy(storeaccounttransaction.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (satq *StoreAccountTransactionQuery) GroupBy(field string, fields ...string) *StoreAccountTransactionGroupBy {
	satq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StoreAccountTransactionGroupBy{build: satq}
	grbuild.flds = &satq.ctx.Fields
	grbuild.label = storeaccounttransaction.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.StoreAccountTransaction.Query().
//		Select(storeaccounttransaction.FieldCreatedAt).
//		Scan(ctx, &v)
func (satq *StoreAccountTransactionQuery) Select(fields ...string) *StoreAccountTransactionSelect {
	satq.ctx.Fields = append(satq.ctx.Fields, fields...)
	sbuild := &StoreAccountTransactionSelect{StoreAccountTransactionQuery: satq}
	sbuild.label = storeaccounttransaction.Label
	sbuild.flds, sbuild.scan = &satq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StoreAccountTransactionSelect configured with the given aggregations.
func (satq *StoreAccountTransactionQuery) Aggregate(fns ...AggregateFunc) *StoreAccountTransactionSelect {
	return satq.Select().Aggregate(fns...)
}

func (satq *StoreAccountTransactionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range satq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, satq); err != nil {
				return err
			}
		}
	}
	for _, f := range satq.ctx.Fields {
		if !storeaccounttransaction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if satq.path != nil {
		prev, err := satq.path(ctx)
		if err != nil {
			return err
		}
		satq.sql = prev
	}
	return nil
}

func (satq *StoreAccountTransactionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StoreAccountTransaction, error) {
	var (
		nodes       = []*StoreAccountTransaction{}
		_spec       = satq.querySpec()
		loadedTypes = [1]bool{
			satq.withStore != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StoreAccountTransaction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StoreAccountTransaction{config: satq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(satq.modifiers) > 0 {
		_spec.Modifiers = satq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, satq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := satq.withStore; query != nil {
		if err := satq.loadStore(ctx, query, nodes, nil,
			func(n *StoreAccountTransaction, e *Store) { n.Edges.Store = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (satq *StoreAccountTransactionQuery) loadStore(ctx context.Context, query *StoreQuery, nodes []*StoreAccountTransaction, init func(*StoreAccountTransaction), assign func(*StoreAccountTransaction, *Store)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*StoreAccountTransaction)
	for i := range nodes {
		fk := nodes[i].StoreID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(store.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "store_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (satq *StoreAccountTransactionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := satq.querySpec()
	if len(satq.modifiers) > 0 {
		_spec.Modifiers = satq.modifiers
	}
	_spec.Node.Columns = satq.ctx.Fields
	if len(satq.ctx.Fields) > 0 {
		_spec.Unique = satq.ctx.Unique != nil && *satq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, satq.driver, _spec)
}

func (satq *StoreAccountTransactionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(storeaccounttransaction.Table, storeaccounttransaction.Columns, sqlgraph.NewFieldSpec(storeaccounttransaction.FieldID, field.TypeInt))
	_spec.From = satq.sql
	if unique := satq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if satq.path != nil {
		_spec.Unique = true
	}
	if fields := satq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, storeaccounttransaction.FieldID)
		for i := range fields {
			if fields[i] != storeaccounttransaction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if satq.withStore != nil {
			_spec.Node.AddColumnOnce(storeaccounttransaction.FieldStoreID)
		}
	}
	if ps := satq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := satq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := satq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := satq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (satq *StoreAccountTransactionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(satq.driver.Dialect())
	t1 := builder.Table(storeaccounttransaction.Table)
	columns := satq.ctx.Fields
	if len(columns) == 0 {
		columns = storeaccounttransaction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if satq.sql != nil {
		selector = satq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if satq.ctx.Unique != nil && *satq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range satq.modifiers {
		m(selector)
	}
	for _, p := range satq.predicates {
		p(selector)
	}
	for _, p := range satq.order {
		p(selector)
	}
	if offset := satq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := satq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (satq *StoreAccountTransactionQuery) ForUpdate(opts ...sql.LockOption) *StoreAccountTransactionQuery {
	if satq.driver.Dialect() == dialect.Postgres {
		satq.Unique(false)
	}
	satq.modifiers = append(satq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return satq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (satq *StoreAccountTransactionQuery) ForShare(opts ...sql.LockOption) *StoreAccountTransactionQuery {
	if satq.driver.Dialect() == dialect.Postgres {
		satq.Unique(false)
	}
	satq.modifiers = append(satq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return satq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (satq *StoreAccountTransactionQuery) Modify(modifiers ...func(s *sql.Selector)) *StoreAccountTransactionSelect {
	satq.modifiers = append(satq.modifiers, modifiers...)
	return satq.Select()
}

// StoreAccountTransactionGroupBy is the group-by builder for StoreAccountTransaction entities.
type StoreAccountTransactionGroupBy struct {
	selector
	build *StoreAccountTransactionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (satgb *StoreAccountTransactionGroupBy) Aggregate(fns ...AggregateFunc) *StoreAccountTransactionGroupBy {
	satgb.fns = append(satgb.fns, fns...)
	return satgb
}

// Scan applies the selector query and scans the result into the given value.
func (satgb *StoreAccountTransactionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, satgb.build.ctx, ent.OpQueryGroupBy)
	if err := satgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StoreAccountTransactionQuery, *StoreAccountTransactionGroupBy](ctx, satgb.build, satgb, satgb.build.inters, v)
}

func (satgb *StoreAccountTransactionGroupBy) sqlScan(ctx context.Context, root *StoreAccountTransactionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(satgb.fns))
	for _, fn := range satgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*satgb.flds)+len(satgb.fns))
		for _, f := range *satgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*satgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := satgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StoreAccountTransactionSelect is the builder for selecting fields of StoreAccountTransaction entities.
type StoreAccountTransactionSelect struct {
	*StoreAccountTransactionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sats *StoreAccountTransactionSelect) Aggregate(fns ...AggregateFunc) *StoreAccountTransactionSelect {
	sats.fns = append(sats.fns, fns...)
	return sats
}

// Scan applies the selector query and scans the result into the given value.
func (sats *StoreAccountTransactionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sats.ctx, ent.OpQuerySelect)
	if err := sats.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StoreAccountTransactionQuery, *StoreAccountTransactionSelect](ctx, sats.StoreAccountTransactionQuery, sats, sats.inters, v)
}

func (sats *StoreAccountTransactionSelect) sqlScan(ctx context.Context, root *StoreAccountTransactionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sats.fns))
	for _, fn := range sats.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sats *StoreAccountTransactionSelect) Modify(modifiers ...func(s *sql.Selector)) *StoreAccountTransactionSelect {
	sats.modifiers = append(sats.modifiers, modifiers...)
	return sats
}
