// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
	"gitlab.jiguang.dev/pos-dine/dine/ent/storeaccounttransaction"
)

// StoreAccountTransaction is the model entity for the StoreAccountTransaction schema.
type StoreAccountTransaction struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 门店ID
	StoreID int `json:"store_id,omitempty"`
	// 单据编号
	No string `json:"no,omitempty"`
	// 变动金额
	Amount decimal.Decimal `json:"amount,omitempty"`
	// 变动后金额
	After decimal.Decimal `json:"after,omitempty"`
	// 变动类型：1-销售进账 2-进账撤回 3-申请提现 4-提现通过 5-提现驳回
	Type domain.TransactionType `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StoreAccountTransactionQuery when eager-loading is set.
	Edges        StoreAccountTransactionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// StoreAccountTransactionEdges holds the relations/edges for other nodes in the graph.
type StoreAccountTransactionEdges struct {
	// Store holds the value of the store edge.
	Store *Store `json:"store,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// StoreOrErr returns the Store value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StoreAccountTransactionEdges) StoreOrErr() (*Store, error) {
	if e.Store != nil {
		return e.Store, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: store.Label}
	}
	return nil, &NotLoadedError{edge: "store"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StoreAccountTransaction) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case storeaccounttransaction.FieldAmount, storeaccounttransaction.FieldAfter:
			values[i] = new(decimal.Decimal)
		case storeaccounttransaction.FieldID, storeaccounttransaction.FieldDeletedAt, storeaccounttransaction.FieldStoreID, storeaccounttransaction.FieldType:
			values[i] = new(sql.NullInt64)
		case storeaccounttransaction.FieldNo:
			values[i] = new(sql.NullString)
		case storeaccounttransaction.FieldCreatedAt, storeaccounttransaction.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StoreAccountTransaction fields.
func (sat *StoreAccountTransaction) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case storeaccounttransaction.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sat.ID = int(value.Int64)
		case storeaccounttransaction.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sat.CreatedAt = value.Time
			}
		case storeaccounttransaction.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sat.UpdatedAt = value.Time
			}
		case storeaccounttransaction.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				sat.DeletedAt = value.Int64
			}
		case storeaccounttransaction.FieldStoreID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value.Valid {
				sat.StoreID = int(value.Int64)
			}
		case storeaccounttransaction.FieldNo:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field no", values[i])
			} else if value.Valid {
				sat.No = value.String
			}
		case storeaccounttransaction.FieldAmount:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value != nil {
				sat.Amount = *value
			}
		case storeaccounttransaction.FieldAfter:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field after", values[i])
			} else if value != nil {
				sat.After = *value
			}
		case storeaccounttransaction.FieldType:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				sat.Type = domain.TransactionType(value.Int64)
			}
		default:
			sat.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StoreAccountTransaction.
// This includes values selected through modifiers, order, etc.
func (sat *StoreAccountTransaction) Value(name string) (ent.Value, error) {
	return sat.selectValues.Get(name)
}

// QueryStore queries the "store" edge of the StoreAccountTransaction entity.
func (sat *StoreAccountTransaction) QueryStore() *StoreQuery {
	return NewStoreAccountTransactionClient(sat.config).QueryStore(sat)
}

// Update returns a builder for updating this StoreAccountTransaction.
// Note that you need to call StoreAccountTransaction.Unwrap() before calling this method if this StoreAccountTransaction
// was returned from a transaction, and the transaction was committed or rolled back.
func (sat *StoreAccountTransaction) Update() *StoreAccountTransactionUpdateOne {
	return NewStoreAccountTransactionClient(sat.config).UpdateOne(sat)
}

// Unwrap unwraps the StoreAccountTransaction entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sat *StoreAccountTransaction) Unwrap() *StoreAccountTransaction {
	_tx, ok := sat.config.driver.(*txDriver)
	if !ok {
		panic("ent: StoreAccountTransaction is not a transactional entity")
	}
	sat.config.driver = _tx.drv
	return sat
}

// String implements the fmt.Stringer.
func (sat *StoreAccountTransaction) String() string {
	var builder strings.Builder
	builder.WriteString("StoreAccountTransaction(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sat.ID))
	builder.WriteString("created_at=")
	builder.WriteString(sat.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sat.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", sat.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", sat.StoreID))
	builder.WriteString(", ")
	builder.WriteString("no=")
	builder.WriteString(sat.No)
	builder.WriteString(", ")
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", sat.Amount))
	builder.WriteString(", ")
	builder.WriteString("after=")
	builder.WriteString(fmt.Sprintf("%v", sat.After))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", sat.Type))
	builder.WriteByte(')')
	return builder.String()
}

// StoreAccountTransactions is a parsable slice of StoreAccountTransaction.
type StoreAccountTransactions []*StoreAccountTransaction
