// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchant"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchantbusinesstype"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
)

// MerchantBusinessTypeQuery is the builder for querying MerchantBusinessType entities.
type MerchantBusinessTypeQuery struct {
	config
	ctx           *QueryContext
	order         []merchantbusinesstype.OrderOption
	inters        []Interceptor
	predicates    []predicate.MerchantBusinessType
	withMerchants *MerchantQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MerchantBusinessTypeQuery builder.
func (mbtq *MerchantBusinessTypeQuery) Where(ps ...predicate.MerchantBusinessType) *MerchantBusinessTypeQuery {
	mbtq.predicates = append(mbtq.predicates, ps...)
	return mbtq
}

// Limit the number of records to be returned by this query.
func (mbtq *MerchantBusinessTypeQuery) Limit(limit int) *MerchantBusinessTypeQuery {
	mbtq.ctx.Limit = &limit
	return mbtq
}

// Offset to start from.
func (mbtq *MerchantBusinessTypeQuery) Offset(offset int) *MerchantBusinessTypeQuery {
	mbtq.ctx.Offset = &offset
	return mbtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mbtq *MerchantBusinessTypeQuery) Unique(unique bool) *MerchantBusinessTypeQuery {
	mbtq.ctx.Unique = &unique
	return mbtq
}

// Order specifies how the records should be ordered.
func (mbtq *MerchantBusinessTypeQuery) Order(o ...merchantbusinesstype.OrderOption) *MerchantBusinessTypeQuery {
	mbtq.order = append(mbtq.order, o...)
	return mbtq
}

// QueryMerchants chains the current query on the "merchants" edge.
func (mbtq *MerchantBusinessTypeQuery) QueryMerchants() *MerchantQuery {
	query := (&MerchantClient{config: mbtq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mbtq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mbtq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(merchantbusinesstype.Table, merchantbusinesstype.FieldID, selector),
			sqlgraph.To(merchant.Table, merchant.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, merchantbusinesstype.MerchantsTable, merchantbusinesstype.MerchantsColumn),
		)
		fromU = sqlgraph.SetNeighbors(mbtq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MerchantBusinessType entity from the query.
// Returns a *NotFoundError when no MerchantBusinessType was found.
func (mbtq *MerchantBusinessTypeQuery) First(ctx context.Context) (*MerchantBusinessType, error) {
	nodes, err := mbtq.Limit(1).All(setContextOp(ctx, mbtq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{merchantbusinesstype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) FirstX(ctx context.Context) *MerchantBusinessType {
	node, err := mbtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MerchantBusinessType ID from the query.
// Returns a *NotFoundError when no MerchantBusinessType ID was found.
func (mbtq *MerchantBusinessTypeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mbtq.Limit(1).IDs(setContextOp(ctx, mbtq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{merchantbusinesstype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) FirstIDX(ctx context.Context) int {
	id, err := mbtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MerchantBusinessType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MerchantBusinessType entity is found.
// Returns a *NotFoundError when no MerchantBusinessType entities are found.
func (mbtq *MerchantBusinessTypeQuery) Only(ctx context.Context) (*MerchantBusinessType, error) {
	nodes, err := mbtq.Limit(2).All(setContextOp(ctx, mbtq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{merchantbusinesstype.Label}
	default:
		return nil, &NotSingularError{merchantbusinesstype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) OnlyX(ctx context.Context) *MerchantBusinessType {
	node, err := mbtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MerchantBusinessType ID in the query.
// Returns a *NotSingularError when more than one MerchantBusinessType ID is found.
// Returns a *NotFoundError when no entities are found.
func (mbtq *MerchantBusinessTypeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mbtq.Limit(2).IDs(setContextOp(ctx, mbtq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{merchantbusinesstype.Label}
	default:
		err = &NotSingularError{merchantbusinesstype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) OnlyIDX(ctx context.Context) int {
	id, err := mbtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MerchantBusinessTypes.
func (mbtq *MerchantBusinessTypeQuery) All(ctx context.Context) ([]*MerchantBusinessType, error) {
	ctx = setContextOp(ctx, mbtq.ctx, ent.OpQueryAll)
	if err := mbtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MerchantBusinessType, *MerchantBusinessTypeQuery]()
	return withInterceptors[[]*MerchantBusinessType](ctx, mbtq, qr, mbtq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) AllX(ctx context.Context) []*MerchantBusinessType {
	nodes, err := mbtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MerchantBusinessType IDs.
func (mbtq *MerchantBusinessTypeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mbtq.ctx.Unique == nil && mbtq.path != nil {
		mbtq.Unique(true)
	}
	ctx = setContextOp(ctx, mbtq.ctx, ent.OpQueryIDs)
	if err = mbtq.Select(merchantbusinesstype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) IDsX(ctx context.Context) []int {
	ids, err := mbtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mbtq *MerchantBusinessTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mbtq.ctx, ent.OpQueryCount)
	if err := mbtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mbtq, querierCount[*MerchantBusinessTypeQuery](), mbtq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) CountX(ctx context.Context) int {
	count, err := mbtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mbtq *MerchantBusinessTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mbtq.ctx, ent.OpQueryExist)
	switch _, err := mbtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mbtq *MerchantBusinessTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := mbtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MerchantBusinessTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mbtq *MerchantBusinessTypeQuery) Clone() *MerchantBusinessTypeQuery {
	if mbtq == nil {
		return nil
	}
	return &MerchantBusinessTypeQuery{
		config:        mbtq.config,
		ctx:           mbtq.ctx.Clone(),
		order:         append([]merchantbusinesstype.OrderOption{}, mbtq.order...),
		inters:        append([]Interceptor{}, mbtq.inters...),
		predicates:    append([]predicate.MerchantBusinessType{}, mbtq.predicates...),
		withMerchants: mbtq.withMerchants.Clone(),
		// clone intermediate query.
		sql:       mbtq.sql.Clone(),
		path:      mbtq.path,
		modifiers: append([]func(*sql.Selector){}, mbtq.modifiers...),
	}
}

// WithMerchants tells the query-builder to eager-load the nodes that are connected to
// the "merchants" edge. The optional arguments are used to configure the query builder of the edge.
func (mbtq *MerchantBusinessTypeQuery) WithMerchants(opts ...func(*MerchantQuery)) *MerchantBusinessTypeQuery {
	query := (&MerchantClient{config: mbtq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mbtq.withMerchants = query
	return mbtq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TypeCode string `json:"type_code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MerchantBusinessType.Query().
//		GroupBy(merchantbusinesstype.FieldTypeCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mbtq *MerchantBusinessTypeQuery) GroupBy(field string, fields ...string) *MerchantBusinessTypeGroupBy {
	mbtq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MerchantBusinessTypeGroupBy{build: mbtq}
	grbuild.flds = &mbtq.ctx.Fields
	grbuild.label = merchantbusinesstype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TypeCode string `json:"type_code,omitempty"`
//	}
//
//	client.MerchantBusinessType.Query().
//		Select(merchantbusinesstype.FieldTypeCode).
//		Scan(ctx, &v)
func (mbtq *MerchantBusinessTypeQuery) Select(fields ...string) *MerchantBusinessTypeSelect {
	mbtq.ctx.Fields = append(mbtq.ctx.Fields, fields...)
	sbuild := &MerchantBusinessTypeSelect{MerchantBusinessTypeQuery: mbtq}
	sbuild.label = merchantbusinesstype.Label
	sbuild.flds, sbuild.scan = &mbtq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MerchantBusinessTypeSelect configured with the given aggregations.
func (mbtq *MerchantBusinessTypeQuery) Aggregate(fns ...AggregateFunc) *MerchantBusinessTypeSelect {
	return mbtq.Select().Aggregate(fns...)
}

func (mbtq *MerchantBusinessTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mbtq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mbtq); err != nil {
				return err
			}
		}
	}
	for _, f := range mbtq.ctx.Fields {
		if !merchantbusinesstype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mbtq.path != nil {
		prev, err := mbtq.path(ctx)
		if err != nil {
			return err
		}
		mbtq.sql = prev
	}
	return nil
}

func (mbtq *MerchantBusinessTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MerchantBusinessType, error) {
	var (
		nodes       = []*MerchantBusinessType{}
		_spec       = mbtq.querySpec()
		loadedTypes = [1]bool{
			mbtq.withMerchants != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MerchantBusinessType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MerchantBusinessType{config: mbtq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mbtq.modifiers) > 0 {
		_spec.Modifiers = mbtq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mbtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mbtq.withMerchants; query != nil {
		if err := mbtq.loadMerchants(ctx, query, nodes,
			func(n *MerchantBusinessType) { n.Edges.Merchants = []*Merchant{} },
			func(n *MerchantBusinessType, e *Merchant) { n.Edges.Merchants = append(n.Edges.Merchants, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mbtq *MerchantBusinessTypeQuery) loadMerchants(ctx context.Context, query *MerchantQuery, nodes []*MerchantBusinessType, init func(*MerchantBusinessType), assign func(*MerchantBusinessType, *Merchant)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*MerchantBusinessType)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(merchant.FieldBusinessTypeID)
	}
	query.Where(predicate.Merchant(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(merchantbusinesstype.MerchantsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BusinessTypeID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "business_type_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (mbtq *MerchantBusinessTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mbtq.querySpec()
	if len(mbtq.modifiers) > 0 {
		_spec.Modifiers = mbtq.modifiers
	}
	_spec.Node.Columns = mbtq.ctx.Fields
	if len(mbtq.ctx.Fields) > 0 {
		_spec.Unique = mbtq.ctx.Unique != nil && *mbtq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mbtq.driver, _spec)
}

func (mbtq *MerchantBusinessTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(merchantbusinesstype.Table, merchantbusinesstype.Columns, sqlgraph.NewFieldSpec(merchantbusinesstype.FieldID, field.TypeInt))
	_spec.From = mbtq.sql
	if unique := mbtq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mbtq.path != nil {
		_spec.Unique = true
	}
	if fields := mbtq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, merchantbusinesstype.FieldID)
		for i := range fields {
			if fields[i] != merchantbusinesstype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mbtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mbtq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mbtq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mbtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mbtq *MerchantBusinessTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mbtq.driver.Dialect())
	t1 := builder.Table(merchantbusinesstype.Table)
	columns := mbtq.ctx.Fields
	if len(columns) == 0 {
		columns = merchantbusinesstype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mbtq.sql != nil {
		selector = mbtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mbtq.ctx.Unique != nil && *mbtq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mbtq.modifiers {
		m(selector)
	}
	for _, p := range mbtq.predicates {
		p(selector)
	}
	for _, p := range mbtq.order {
		p(selector)
	}
	if offset := mbtq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mbtq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (mbtq *MerchantBusinessTypeQuery) ForUpdate(opts ...sql.LockOption) *MerchantBusinessTypeQuery {
	if mbtq.driver.Dialect() == dialect.Postgres {
		mbtq.Unique(false)
	}
	mbtq.modifiers = append(mbtq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return mbtq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (mbtq *MerchantBusinessTypeQuery) ForShare(opts ...sql.LockOption) *MerchantBusinessTypeQuery {
	if mbtq.driver.Dialect() == dialect.Postgres {
		mbtq.Unique(false)
	}
	mbtq.modifiers = append(mbtq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return mbtq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mbtq *MerchantBusinessTypeQuery) Modify(modifiers ...func(s *sql.Selector)) *MerchantBusinessTypeSelect {
	mbtq.modifiers = append(mbtq.modifiers, modifiers...)
	return mbtq.Select()
}

// MerchantBusinessTypeGroupBy is the group-by builder for MerchantBusinessType entities.
type MerchantBusinessTypeGroupBy struct {
	selector
	build *MerchantBusinessTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mbtgb *MerchantBusinessTypeGroupBy) Aggregate(fns ...AggregateFunc) *MerchantBusinessTypeGroupBy {
	mbtgb.fns = append(mbtgb.fns, fns...)
	return mbtgb
}

// Scan applies the selector query and scans the result into the given value.
func (mbtgb *MerchantBusinessTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mbtgb.build.ctx, ent.OpQueryGroupBy)
	if err := mbtgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MerchantBusinessTypeQuery, *MerchantBusinessTypeGroupBy](ctx, mbtgb.build, mbtgb, mbtgb.build.inters, v)
}

func (mbtgb *MerchantBusinessTypeGroupBy) sqlScan(ctx context.Context, root *MerchantBusinessTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mbtgb.fns))
	for _, fn := range mbtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mbtgb.flds)+len(mbtgb.fns))
		for _, f := range *mbtgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mbtgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mbtgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MerchantBusinessTypeSelect is the builder for selecting fields of MerchantBusinessType entities.
type MerchantBusinessTypeSelect struct {
	*MerchantBusinessTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mbts *MerchantBusinessTypeSelect) Aggregate(fns ...AggregateFunc) *MerchantBusinessTypeSelect {
	mbts.fns = append(mbts.fns, fns...)
	return mbts
}

// Scan applies the selector query and scans the result into the given value.
func (mbts *MerchantBusinessTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mbts.ctx, ent.OpQuerySelect)
	if err := mbts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MerchantBusinessTypeQuery, *MerchantBusinessTypeSelect](ctx, mbts.MerchantBusinessTypeQuery, mbts, mbts.inters, v)
}

func (mbts *MerchantBusinessTypeSelect) sqlScan(ctx context.Context, root *MerchantBusinessTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mbts.fns))
	for _, fn := range mbts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mbts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mbts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mbts *MerchantBusinessTypeSelect) Modify(modifiers ...func(s *sql.Selector)) *MerchantBusinessTypeSelect {
	mbts.modifiers = append(mbts.modifiers, modifiers...)
	return mbts
}
