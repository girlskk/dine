// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchant"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchantrenewal"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
)

// MerchantRenewalQuery is the builder for querying MerchantRenewal entities.
type MerchantRenewalQuery struct {
	config
	ctx          *QueryContext
	order        []merchantrenewal.OrderOption
	inters       []Interceptor
	predicates   []predicate.MerchantRenewal
	withMerchant *MerchantQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MerchantRenewalQuery builder.
func (mrq *MerchantRenewalQuery) Where(ps ...predicate.MerchantRenewal) *MerchantRenewalQuery {
	mrq.predicates = append(mrq.predicates, ps...)
	return mrq
}

// Limit the number of records to be returned by this query.
func (mrq *MerchantRenewalQuery) Limit(limit int) *MerchantRenewalQuery {
	mrq.ctx.Limit = &limit
	return mrq
}

// Offset to start from.
func (mrq *MerchantRenewalQuery) Offset(offset int) *MerchantRenewalQuery {
	mrq.ctx.Offset = &offset
	return mrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mrq *MerchantRenewalQuery) Unique(unique bool) *MerchantRenewalQuery {
	mrq.ctx.Unique = &unique
	return mrq
}

// Order specifies how the records should be ordered.
func (mrq *MerchantRenewalQuery) Order(o ...merchantrenewal.OrderOption) *MerchantRenewalQuery {
	mrq.order = append(mrq.order, o...)
	return mrq
}

// QueryMerchant chains the current query on the "merchant" edge.
func (mrq *MerchantRenewalQuery) QueryMerchant() *MerchantQuery {
	query := (&MerchantClient{config: mrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(merchantrenewal.Table, merchantrenewal.FieldID, selector),
			sqlgraph.To(merchant.Table, merchant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, merchantrenewal.MerchantTable, merchantrenewal.MerchantColumn),
		)
		fromU = sqlgraph.SetNeighbors(mrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MerchantRenewal entity from the query.
// Returns a *NotFoundError when no MerchantRenewal was found.
func (mrq *MerchantRenewalQuery) First(ctx context.Context) (*MerchantRenewal, error) {
	nodes, err := mrq.Limit(1).All(setContextOp(ctx, mrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{merchantrenewal.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) FirstX(ctx context.Context) *MerchantRenewal {
	node, err := mrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MerchantRenewal ID from the query.
// Returns a *NotFoundError when no MerchantRenewal ID was found.
func (mrq *MerchantRenewalQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mrq.Limit(1).IDs(setContextOp(ctx, mrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{merchantrenewal.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) FirstIDX(ctx context.Context) int {
	id, err := mrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MerchantRenewal entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MerchantRenewal entity is found.
// Returns a *NotFoundError when no MerchantRenewal entities are found.
func (mrq *MerchantRenewalQuery) Only(ctx context.Context) (*MerchantRenewal, error) {
	nodes, err := mrq.Limit(2).All(setContextOp(ctx, mrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{merchantrenewal.Label}
	default:
		return nil, &NotSingularError{merchantrenewal.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) OnlyX(ctx context.Context) *MerchantRenewal {
	node, err := mrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MerchantRenewal ID in the query.
// Returns a *NotSingularError when more than one MerchantRenewal ID is found.
// Returns a *NotFoundError when no entities are found.
func (mrq *MerchantRenewalQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mrq.Limit(2).IDs(setContextOp(ctx, mrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{merchantrenewal.Label}
	default:
		err = &NotSingularError{merchantrenewal.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) OnlyIDX(ctx context.Context) int {
	id, err := mrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MerchantRenewals.
func (mrq *MerchantRenewalQuery) All(ctx context.Context) ([]*MerchantRenewal, error) {
	ctx = setContextOp(ctx, mrq.ctx, ent.OpQueryAll)
	if err := mrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MerchantRenewal, *MerchantRenewalQuery]()
	return withInterceptors[[]*MerchantRenewal](ctx, mrq, qr, mrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) AllX(ctx context.Context) []*MerchantRenewal {
	nodes, err := mrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MerchantRenewal IDs.
func (mrq *MerchantRenewalQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mrq.ctx.Unique == nil && mrq.path != nil {
		mrq.Unique(true)
	}
	ctx = setContextOp(ctx, mrq.ctx, ent.OpQueryIDs)
	if err = mrq.Select(merchantrenewal.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) IDsX(ctx context.Context) []int {
	ids, err := mrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mrq *MerchantRenewalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mrq.ctx, ent.OpQueryCount)
	if err := mrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mrq, querierCount[*MerchantRenewalQuery](), mrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) CountX(ctx context.Context) int {
	count, err := mrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mrq *MerchantRenewalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mrq.ctx, ent.OpQueryExist)
	switch _, err := mrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mrq *MerchantRenewalQuery) ExistX(ctx context.Context) bool {
	exist, err := mrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MerchantRenewalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mrq *MerchantRenewalQuery) Clone() *MerchantRenewalQuery {
	if mrq == nil {
		return nil
	}
	return &MerchantRenewalQuery{
		config:       mrq.config,
		ctx:          mrq.ctx.Clone(),
		order:        append([]merchantrenewal.OrderOption{}, mrq.order...),
		inters:       append([]Interceptor{}, mrq.inters...),
		predicates:   append([]predicate.MerchantRenewal{}, mrq.predicates...),
		withMerchant: mrq.withMerchant.Clone(),
		// clone intermediate query.
		sql:       mrq.sql.Clone(),
		path:      mrq.path,
		modifiers: append([]func(*sql.Selector){}, mrq.modifiers...),
	}
}

// WithMerchant tells the query-builder to eager-load the nodes that are connected to
// the "merchant" edge. The optional arguments are used to configure the query builder of the edge.
func (mrq *MerchantRenewalQuery) WithMerchant(opts ...func(*MerchantQuery)) *MerchantRenewalQuery {
	query := (&MerchantClient{config: mrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mrq.withMerchant = query
	return mrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MerchantRenewal.Query().
//		GroupBy(merchantrenewal.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mrq *MerchantRenewalQuery) GroupBy(field string, fields ...string) *MerchantRenewalGroupBy {
	mrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MerchantRenewalGroupBy{build: mrq}
	grbuild.flds = &mrq.ctx.Fields
	grbuild.label = merchantrenewal.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.MerchantRenewal.Query().
//		Select(merchantrenewal.FieldCreatedAt).
//		Scan(ctx, &v)
func (mrq *MerchantRenewalQuery) Select(fields ...string) *MerchantRenewalSelect {
	mrq.ctx.Fields = append(mrq.ctx.Fields, fields...)
	sbuild := &MerchantRenewalSelect{MerchantRenewalQuery: mrq}
	sbuild.label = merchantrenewal.Label
	sbuild.flds, sbuild.scan = &mrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MerchantRenewalSelect configured with the given aggregations.
func (mrq *MerchantRenewalQuery) Aggregate(fns ...AggregateFunc) *MerchantRenewalSelect {
	return mrq.Select().Aggregate(fns...)
}

func (mrq *MerchantRenewalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mrq); err != nil {
				return err
			}
		}
	}
	for _, f := range mrq.ctx.Fields {
		if !merchantrenewal.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mrq.path != nil {
		prev, err := mrq.path(ctx)
		if err != nil {
			return err
		}
		mrq.sql = prev
	}
	return nil
}

func (mrq *MerchantRenewalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MerchantRenewal, error) {
	var (
		nodes       = []*MerchantRenewal{}
		_spec       = mrq.querySpec()
		loadedTypes = [1]bool{
			mrq.withMerchant != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MerchantRenewal).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MerchantRenewal{config: mrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(mrq.modifiers) > 0 {
		_spec.Modifiers = mrq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mrq.withMerchant; query != nil {
		if err := mrq.loadMerchant(ctx, query, nodes, nil,
			func(n *MerchantRenewal, e *Merchant) { n.Edges.Merchant = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mrq *MerchantRenewalQuery) loadMerchant(ctx context.Context, query *MerchantQuery, nodes []*MerchantRenewal, init func(*MerchantRenewal), assign func(*MerchantRenewal, *Merchant)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*MerchantRenewal)
	for i := range nodes {
		fk := nodes[i].MerchantID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(merchant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "merchant_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (mrq *MerchantRenewalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mrq.querySpec()
	if len(mrq.modifiers) > 0 {
		_spec.Modifiers = mrq.modifiers
	}
	_spec.Node.Columns = mrq.ctx.Fields
	if len(mrq.ctx.Fields) > 0 {
		_spec.Unique = mrq.ctx.Unique != nil && *mrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mrq.driver, _spec)
}

func (mrq *MerchantRenewalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(merchantrenewal.Table, merchantrenewal.Columns, sqlgraph.NewFieldSpec(merchantrenewal.FieldID, field.TypeInt))
	_spec.From = mrq.sql
	if unique := mrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mrq.path != nil {
		_spec.Unique = true
	}
	if fields := mrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, merchantrenewal.FieldID)
		for i := range fields {
			if fields[i] != merchantrenewal.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if mrq.withMerchant != nil {
			_spec.Node.AddColumnOnce(merchantrenewal.FieldMerchantID)
		}
	}
	if ps := mrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mrq *MerchantRenewalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mrq.driver.Dialect())
	t1 := builder.Table(merchantrenewal.Table)
	columns := mrq.ctx.Fields
	if len(columns) == 0 {
		columns = merchantrenewal.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mrq.sql != nil {
		selector = mrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mrq.ctx.Unique != nil && *mrq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mrq.modifiers {
		m(selector)
	}
	for _, p := range mrq.predicates {
		p(selector)
	}
	for _, p := range mrq.order {
		p(selector)
	}
	if offset := mrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (mrq *MerchantRenewalQuery) ForUpdate(opts ...sql.LockOption) *MerchantRenewalQuery {
	if mrq.driver.Dialect() == dialect.Postgres {
		mrq.Unique(false)
	}
	mrq.modifiers = append(mrq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return mrq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (mrq *MerchantRenewalQuery) ForShare(opts ...sql.LockOption) *MerchantRenewalQuery {
	if mrq.driver.Dialect() == dialect.Postgres {
		mrq.Unique(false)
	}
	mrq.modifiers = append(mrq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return mrq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mrq *MerchantRenewalQuery) Modify(modifiers ...func(s *sql.Selector)) *MerchantRenewalSelect {
	mrq.modifiers = append(mrq.modifiers, modifiers...)
	return mrq.Select()
}

// MerchantRenewalGroupBy is the group-by builder for MerchantRenewal entities.
type MerchantRenewalGroupBy struct {
	selector
	build *MerchantRenewalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mrgb *MerchantRenewalGroupBy) Aggregate(fns ...AggregateFunc) *MerchantRenewalGroupBy {
	mrgb.fns = append(mrgb.fns, fns...)
	return mrgb
}

// Scan applies the selector query and scans the result into the given value.
func (mrgb *MerchantRenewalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mrgb.build.ctx, ent.OpQueryGroupBy)
	if err := mrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MerchantRenewalQuery, *MerchantRenewalGroupBy](ctx, mrgb.build, mrgb, mrgb.build.inters, v)
}

func (mrgb *MerchantRenewalGroupBy) sqlScan(ctx context.Context, root *MerchantRenewalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mrgb.fns))
	for _, fn := range mrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mrgb.flds)+len(mrgb.fns))
		for _, f := range *mrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MerchantRenewalSelect is the builder for selecting fields of MerchantRenewal entities.
type MerchantRenewalSelect struct {
	*MerchantRenewalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mrs *MerchantRenewalSelect) Aggregate(fns ...AggregateFunc) *MerchantRenewalSelect {
	mrs.fns = append(mrs.fns, fns...)
	return mrs
}

// Scan applies the selector query and scans the result into the given value.
func (mrs *MerchantRenewalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mrs.ctx, ent.OpQuerySelect)
	if err := mrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MerchantRenewalQuery, *MerchantRenewalSelect](ctx, mrs.MerchantRenewalQuery, mrs, mrs.inters, v)
}

func (mrs *MerchantRenewalSelect) sqlScan(ctx context.Context, root *MerchantRenewalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mrs.fns))
	for _, fn := range mrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mrs *MerchantRenewalSelect) Modify(modifiers ...func(s *sql.Selector)) *MerchantRenewalSelect {
	mrs.modifiers = append(mrs.modifiers, modifiers...)
	return mrs
}
