// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/routermenu"
)

// RouterMenu is the model entity for the RouterMenu schema.
type RouterMenu struct {
	config `json:"-"`
	// ID of the ent.
	// UUID as primary key
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 用户类型：admin/backend/store
	UserType domain.UserType `json:"user_type,omitempty"`
	// 父菜单ID，空表示根节点
	ParentID uuid.UUID `json:"parent_id,omitempty"`
	// 菜单名称
	Name string `json:"name,omitempty"`
	// 前端路由路径
	Path string `json:"path,omitempty"`
	// 菜单层级
	Layer int `json:"layer,omitempty"`
	// 前端组件标识
	Component string `json:"component,omitempty"`
	// 菜单图标
	Icon string `json:"icon,omitempty"`
	// 排序，值越小越靠前
	Sort int `json:"sort,omitempty"`
	// 是否启用
	Enabled bool `json:"enabled,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RouterMenuQuery when eager-loading is set.
	Edges        RouterMenuEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RouterMenuEdges holds the relations/edges for other nodes in the graph.
type RouterMenuEdges struct {
	// 菜单下的权限点
	Permissions []*Permission `json:"permissions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PermissionsOrErr returns the Permissions value or an error if the edge
// was not loaded in eager-loading.
func (e RouterMenuEdges) PermissionsOrErr() ([]*Permission, error) {
	if e.loadedTypes[0] {
		return e.Permissions, nil
	}
	return nil, &NotLoadedError{edge: "permissions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RouterMenu) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case routermenu.FieldEnabled:
			values[i] = new(sql.NullBool)
		case routermenu.FieldDeletedAt, routermenu.FieldLayer, routermenu.FieldSort:
			values[i] = new(sql.NullInt64)
		case routermenu.FieldUserType, routermenu.FieldName, routermenu.FieldPath, routermenu.FieldComponent, routermenu.FieldIcon:
			values[i] = new(sql.NullString)
		case routermenu.FieldCreatedAt, routermenu.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case routermenu.FieldID, routermenu.FieldParentID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RouterMenu fields.
func (rm *RouterMenu) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case routermenu.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				rm.ID = *value
			}
		case routermenu.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rm.CreatedAt = value.Time
			}
		case routermenu.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rm.UpdatedAt = value.Time
			}
		case routermenu.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				rm.DeletedAt = value.Int64
			}
		case routermenu.FieldUserType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_type", values[i])
			} else if value.Valid {
				rm.UserType = domain.UserType(value.String)
			}
		case routermenu.FieldParentID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field parent_id", values[i])
			} else if value != nil {
				rm.ParentID = *value
			}
		case routermenu.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				rm.Name = value.String
			}
		case routermenu.FieldPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value.Valid {
				rm.Path = value.String
			}
		case routermenu.FieldLayer:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field layer", values[i])
			} else if value.Valid {
				rm.Layer = int(value.Int64)
			}
		case routermenu.FieldComponent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field component", values[i])
			} else if value.Valid {
				rm.Component = value.String
			}
		case routermenu.FieldIcon:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field icon", values[i])
			} else if value.Valid {
				rm.Icon = value.String
			}
		case routermenu.FieldSort:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort", values[i])
			} else if value.Valid {
				rm.Sort = int(value.Int64)
			}
		case routermenu.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				rm.Enabled = value.Bool
			}
		default:
			rm.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RouterMenu.
// This includes values selected through modifiers, order, etc.
func (rm *RouterMenu) Value(name string) (ent.Value, error) {
	return rm.selectValues.Get(name)
}

// QueryPermissions queries the "permissions" edge of the RouterMenu entity.
func (rm *RouterMenu) QueryPermissions() *PermissionQuery {
	return NewRouterMenuClient(rm.config).QueryPermissions(rm)
}

// Update returns a builder for updating this RouterMenu.
// Note that you need to call RouterMenu.Unwrap() before calling this method if this RouterMenu
// was returned from a transaction, and the transaction was committed or rolled back.
func (rm *RouterMenu) Update() *RouterMenuUpdateOne {
	return NewRouterMenuClient(rm.config).UpdateOne(rm)
}

// Unwrap unwraps the RouterMenu entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rm *RouterMenu) Unwrap() *RouterMenu {
	_tx, ok := rm.config.driver.(*txDriver)
	if !ok {
		panic("ent: RouterMenu is not a transactional entity")
	}
	rm.config.driver = _tx.drv
	return rm
}

// String implements the fmt.Stringer.
func (rm *RouterMenu) String() string {
	var builder strings.Builder
	builder.WriteString("RouterMenu(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rm.ID))
	builder.WriteString("created_at=")
	builder.WriteString(rm.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rm.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", rm.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("user_type=")
	builder.WriteString(fmt.Sprintf("%v", rm.UserType))
	builder.WriteString(", ")
	builder.WriteString("parent_id=")
	builder.WriteString(fmt.Sprintf("%v", rm.ParentID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(rm.Name)
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(rm.Path)
	builder.WriteString(", ")
	builder.WriteString("layer=")
	builder.WriteString(fmt.Sprintf("%v", rm.Layer))
	builder.WriteString(", ")
	builder.WriteString("component=")
	builder.WriteString(rm.Component)
	builder.WriteString(", ")
	builder.WriteString("icon=")
	builder.WriteString(rm.Icon)
	builder.WriteString(", ")
	builder.WriteString("sort=")
	builder.WriteString(fmt.Sprintf("%v", rm.Sort))
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", rm.Enabled))
	builder.WriteByte(')')
	return builder.String()
}

// RouterMenus is a parsable slice of RouterMenu.
type RouterMenus []*RouterMenu
