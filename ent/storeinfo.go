// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
	"gitlab.jiguang.dev/pos-dine/dine/ent/storeinfo"
)

// StoreInfo is the model entity for the StoreInfo schema.
type StoreInfo struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 省市地区
	City string `json:"city,omitempty"`
	// 详细地址
	Address string `json:"address,omitempty"`
	// 门店联系人
	ContactName string `json:"contact_name,omitempty"`
	// 联系人电话
	ContactPhone string `json:"contact_phone,omitempty"`
	// 门店图片
	Images domain.StoreInfoImages `json:"images,omitempty"`
	// StoreID holds the value of the "store_id" field.
	StoreID int `json:"store_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StoreInfoQuery when eager-loading is set.
	Edges        StoreInfoEdges `json:"edges"`
	selectValues sql.SelectValues
}

// StoreInfoEdges holds the relations/edges for other nodes in the graph.
type StoreInfoEdges struct {
	// Store holds the value of the store edge.
	Store *Store `json:"store,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// StoreOrErr returns the Store value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StoreInfoEdges) StoreOrErr() (*Store, error) {
	if e.Store != nil {
		return e.Store, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: store.Label}
	}
	return nil, &NotLoadedError{edge: "store"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StoreInfo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case storeinfo.FieldImages:
			values[i] = new([]byte)
		case storeinfo.FieldID, storeinfo.FieldStoreID:
			values[i] = new(sql.NullInt64)
		case storeinfo.FieldCity, storeinfo.FieldAddress, storeinfo.FieldContactName, storeinfo.FieldContactPhone:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StoreInfo fields.
func (si *StoreInfo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case storeinfo.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			si.ID = int(value.Int64)
		case storeinfo.FieldCity:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field city", values[i])
			} else if value.Valid {
				si.City = value.String
			}
		case storeinfo.FieldAddress:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field address", values[i])
			} else if value.Valid {
				si.Address = value.String
			}
		case storeinfo.FieldContactName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field contact_name", values[i])
			} else if value.Valid {
				si.ContactName = value.String
			}
		case storeinfo.FieldContactPhone:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field contact_phone", values[i])
			} else if value.Valid {
				si.ContactPhone = value.String
			}
		case storeinfo.FieldImages:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field images", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &si.Images); err != nil {
					return fmt.Errorf("unmarshal field images: %w", err)
				}
			}
		case storeinfo.FieldStoreID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value.Valid {
				si.StoreID = int(value.Int64)
			}
		default:
			si.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StoreInfo.
// This includes values selected through modifiers, order, etc.
func (si *StoreInfo) Value(name string) (ent.Value, error) {
	return si.selectValues.Get(name)
}

// QueryStore queries the "store" edge of the StoreInfo entity.
func (si *StoreInfo) QueryStore() *StoreQuery {
	return NewStoreInfoClient(si.config).QueryStore(si)
}

// Update returns a builder for updating this StoreInfo.
// Note that you need to call StoreInfo.Unwrap() before calling this method if this StoreInfo
// was returned from a transaction, and the transaction was committed or rolled back.
func (si *StoreInfo) Update() *StoreInfoUpdateOne {
	return NewStoreInfoClient(si.config).UpdateOne(si)
}

// Unwrap unwraps the StoreInfo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (si *StoreInfo) Unwrap() *StoreInfo {
	_tx, ok := si.config.driver.(*txDriver)
	if !ok {
		panic("ent: StoreInfo is not a transactional entity")
	}
	si.config.driver = _tx.drv
	return si
}

// String implements the fmt.Stringer.
func (si *StoreInfo) String() string {
	var builder strings.Builder
	builder.WriteString("StoreInfo(")
	builder.WriteString(fmt.Sprintf("id=%v, ", si.ID))
	builder.WriteString("city=")
	builder.WriteString(si.City)
	builder.WriteString(", ")
	builder.WriteString("address=")
	builder.WriteString(si.Address)
	builder.WriteString(", ")
	builder.WriteString("contact_name=")
	builder.WriteString(si.ContactName)
	builder.WriteString(", ")
	builder.WriteString("contact_phone=")
	builder.WriteString(si.ContactPhone)
	builder.WriteString(", ")
	builder.WriteString("images=")
	builder.WriteString(fmt.Sprintf("%v", si.Images))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", si.StoreID))
	builder.WriteByte(')')
	return builder.String()
}

// StoreInfos is a parsable slice of StoreInfo.
type StoreInfos []*StoreInfo
