// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/product"
	"gitlab.jiguang.dev/pos-dine/dine/ent/setmealgroup"
)

// SetMealGroup is the model entity for the SetMealGroup schema.
type SetMealGroup struct {
	config `json:"-"`
	// ID of the ent.
	// UUID as primary key
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 套餐商品ID（外键）
	ProductID uuid.UUID `json:"product_id,omitempty"`
	// 套餐组名称（必选）
	Name string `json:"name,omitempty"`
	// 点单限制：fixed（固定分组）、optional（可选套餐）
	SelectionType domain.SetMealGroupSelectionType `json:"selection_type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SetMealGroupQuery when eager-loading is set.
	Edges        SetMealGroupEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SetMealGroupEdges holds the relations/edges for other nodes in the graph.
type SetMealGroupEdges struct {
	// 所属套餐商品
	Product *Product `json:"product,omitempty"`
	// 套餐组详情列表
	Details []*SetMealDetail `json:"details,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProductOrErr returns the Product value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SetMealGroupEdges) ProductOrErr() (*Product, error) {
	if e.Product != nil {
		return e.Product, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: product.Label}
	}
	return nil, &NotLoadedError{edge: "product"}
}

// DetailsOrErr returns the Details value or an error if the edge
// was not loaded in eager-loading.
func (e SetMealGroupEdges) DetailsOrErr() ([]*SetMealDetail, error) {
	if e.loadedTypes[1] {
		return e.Details, nil
	}
	return nil, &NotLoadedError{edge: "details"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SetMealGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case setmealgroup.FieldDeletedAt:
			values[i] = new(sql.NullInt64)
		case setmealgroup.FieldName, setmealgroup.FieldSelectionType:
			values[i] = new(sql.NullString)
		case setmealgroup.FieldCreatedAt, setmealgroup.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case setmealgroup.FieldID, setmealgroup.FieldProductID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SetMealGroup fields.
func (smg *SetMealGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case setmealgroup.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				smg.ID = *value
			}
		case setmealgroup.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				smg.CreatedAt = value.Time
			}
		case setmealgroup.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				smg.UpdatedAt = value.Time
			}
		case setmealgroup.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				smg.DeletedAt = value.Int64
			}
		case setmealgroup.FieldProductID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value != nil {
				smg.ProductID = *value
			}
		case setmealgroup.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				smg.Name = value.String
			}
		case setmealgroup.FieldSelectionType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field selection_type", values[i])
			} else if value.Valid {
				smg.SelectionType = domain.SetMealGroupSelectionType(value.String)
			}
		default:
			smg.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SetMealGroup.
// This includes values selected through modifiers, order, etc.
func (smg *SetMealGroup) Value(name string) (ent.Value, error) {
	return smg.selectValues.Get(name)
}

// QueryProduct queries the "product" edge of the SetMealGroup entity.
func (smg *SetMealGroup) QueryProduct() *ProductQuery {
	return NewSetMealGroupClient(smg.config).QueryProduct(smg)
}

// QueryDetails queries the "details" edge of the SetMealGroup entity.
func (smg *SetMealGroup) QueryDetails() *SetMealDetailQuery {
	return NewSetMealGroupClient(smg.config).QueryDetails(smg)
}

// Update returns a builder for updating this SetMealGroup.
// Note that you need to call SetMealGroup.Unwrap() before calling this method if this SetMealGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (smg *SetMealGroup) Update() *SetMealGroupUpdateOne {
	return NewSetMealGroupClient(smg.config).UpdateOne(smg)
}

// Unwrap unwraps the SetMealGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (smg *SetMealGroup) Unwrap() *SetMealGroup {
	_tx, ok := smg.config.driver.(*txDriver)
	if !ok {
		panic("ent: SetMealGroup is not a transactional entity")
	}
	smg.config.driver = _tx.drv
	return smg
}

// String implements the fmt.Stringer.
func (smg *SetMealGroup) String() string {
	var builder strings.Builder
	builder.WriteString("SetMealGroup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", smg.ID))
	builder.WriteString("created_at=")
	builder.WriteString(smg.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(smg.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", smg.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", smg.ProductID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(smg.Name)
	builder.WriteString(", ")
	builder.WriteString("selection_type=")
	builder.WriteString(fmt.Sprintf("%v", smg.SelectionType))
	builder.WriteByte(')')
	return builder.String()
}

// SetMealGroups is a parsable slice of SetMealGroup.
type SetMealGroups []*SetMealGroup
