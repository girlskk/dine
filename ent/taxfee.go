// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/shopspring/decimal"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchant"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
	"gitlab.jiguang.dev/pos-dine/dine/ent/taxfee"
)

// TaxFee is the model entity for the TaxFee schema.
type TaxFee struct {
	config `json:"-"`
	// ID of the ent.
	// UUID as primary key
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 税费名称，长度不大于50字
	Name string `json:"name,omitempty"`
	// 税费类型：商户/门店
	TaxFeeType domain.TaxFeeType `json:"tax_fee_type,omitempty"`
	// 税费代码，长度不大于20字
	TaxCode string `json:"tax_code,omitempty"`
	// 税率类型：统一比例/自定义比例
	TaxRateType domain.TaxRateType `json:"tax_rate_type,omitempty"`
	// 税率，6% -> 0.06
	TaxRate decimal.Decimal `json:"tax_rate,omitempty"`
	// 是否默认税费
	DefaultTax bool `json:"default_tax,omitempty"`
	// 品牌商 ID
	MerchantID uuid.UUID `json:"merchant_id,omitempty"`
	// 门店ID，可为空表示品牌维度
	StoreID uuid.UUID `json:"store_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TaxFeeQuery when eager-loading is set.
	Edges        TaxFeeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TaxFeeEdges holds the relations/edges for other nodes in the graph.
type TaxFeeEdges struct {
	// Merchant holds the value of the merchant edge.
	Merchant *Merchant `json:"merchant,omitempty"`
	// Store holds the value of the store edge.
	Store *Store `json:"store,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// MerchantOrErr returns the Merchant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TaxFeeEdges) MerchantOrErr() (*Merchant, error) {
	if e.Merchant != nil {
		return e.Merchant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: merchant.Label}
	}
	return nil, &NotLoadedError{edge: "merchant"}
}

// StoreOrErr returns the Store value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TaxFeeEdges) StoreOrErr() (*Store, error) {
	if e.Store != nil {
		return e.Store, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: store.Label}
	}
	return nil, &NotLoadedError{edge: "store"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TaxFee) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taxfee.FieldTaxRate:
			values[i] = new(decimal.Decimal)
		case taxfee.FieldDefaultTax:
			values[i] = new(sql.NullBool)
		case taxfee.FieldDeletedAt:
			values[i] = new(sql.NullInt64)
		case taxfee.FieldName, taxfee.FieldTaxFeeType, taxfee.FieldTaxCode, taxfee.FieldTaxRateType:
			values[i] = new(sql.NullString)
		case taxfee.FieldCreatedAt, taxfee.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case taxfee.FieldID, taxfee.FieldMerchantID, taxfee.FieldStoreID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TaxFee fields.
func (tf *TaxFee) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taxfee.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tf.ID = *value
			}
		case taxfee.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tf.CreatedAt = value.Time
			}
		case taxfee.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tf.UpdatedAt = value.Time
			}
		case taxfee.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				tf.DeletedAt = value.Int64
			}
		case taxfee.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				tf.Name = value.String
			}
		case taxfee.FieldTaxFeeType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tax_fee_type", values[i])
			} else if value.Valid {
				tf.TaxFeeType = domain.TaxFeeType(value.String)
			}
		case taxfee.FieldTaxCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tax_code", values[i])
			} else if value.Valid {
				tf.TaxCode = value.String
			}
		case taxfee.FieldTaxRateType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tax_rate_type", values[i])
			} else if value.Valid {
				tf.TaxRateType = domain.TaxRateType(value.String)
			}
		case taxfee.FieldTaxRate:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field tax_rate", values[i])
			} else if value != nil {
				tf.TaxRate = *value
			}
		case taxfee.FieldDefaultTax:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field default_tax", values[i])
			} else if value.Valid {
				tf.DefaultTax = value.Bool
			}
		case taxfee.FieldMerchantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field merchant_id", values[i])
			} else if value != nil {
				tf.MerchantID = *value
			}
		case taxfee.FieldStoreID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value != nil {
				tf.StoreID = *value
			}
		default:
			tf.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TaxFee.
// This includes values selected through modifiers, order, etc.
func (tf *TaxFee) Value(name string) (ent.Value, error) {
	return tf.selectValues.Get(name)
}

// QueryMerchant queries the "merchant" edge of the TaxFee entity.
func (tf *TaxFee) QueryMerchant() *MerchantQuery {
	return NewTaxFeeClient(tf.config).QueryMerchant(tf)
}

// QueryStore queries the "store" edge of the TaxFee entity.
func (tf *TaxFee) QueryStore() *StoreQuery {
	return NewTaxFeeClient(tf.config).QueryStore(tf)
}

// Update returns a builder for updating this TaxFee.
// Note that you need to call TaxFee.Unwrap() before calling this method if this TaxFee
// was returned from a transaction, and the transaction was committed or rolled back.
func (tf *TaxFee) Update() *TaxFeeUpdateOne {
	return NewTaxFeeClient(tf.config).UpdateOne(tf)
}

// Unwrap unwraps the TaxFee entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tf *TaxFee) Unwrap() *TaxFee {
	_tx, ok := tf.config.driver.(*txDriver)
	if !ok {
		panic("ent: TaxFee is not a transactional entity")
	}
	tf.config.driver = _tx.drv
	return tf
}

// String implements the fmt.Stringer.
func (tf *TaxFee) String() string {
	var builder strings.Builder
	builder.WriteString("TaxFee(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tf.ID))
	builder.WriteString("created_at=")
	builder.WriteString(tf.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(tf.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", tf.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(tf.Name)
	builder.WriteString(", ")
	builder.WriteString("tax_fee_type=")
	builder.WriteString(fmt.Sprintf("%v", tf.TaxFeeType))
	builder.WriteString(", ")
	builder.WriteString("tax_code=")
	builder.WriteString(tf.TaxCode)
	builder.WriteString(", ")
	builder.WriteString("tax_rate_type=")
	builder.WriteString(fmt.Sprintf("%v", tf.TaxRateType))
	builder.WriteString(", ")
	builder.WriteString("tax_rate=")
	builder.WriteString(fmt.Sprintf("%v", tf.TaxRate))
	builder.WriteString(", ")
	builder.WriteString("default_tax=")
	builder.WriteString(fmt.Sprintf("%v", tf.DefaultTax))
	builder.WriteString(", ")
	builder.WriteString("merchant_id=")
	builder.WriteString(fmt.Sprintf("%v", tf.MerchantID))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", tf.StoreID))
	builder.WriteByte(')')
	return builder.String()
}

// TaxFees is a parsable slice of TaxFee.
type TaxFees []*TaxFee
