// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/merchant"
	"gitlab.jiguang.dev/pos-dine/dine/ent/stall"
	"gitlab.jiguang.dev/pos-dine/dine/ent/store"
)

// Stall is the model entity for the Stall schema.
type Stall struct {
	config `json:"-"`
	// ID of the ent.
	// UUID as primary key
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 出品部门名称，长度不超过20字
	Name string `json:"name,omitempty"`
	// 出品部门类型：系统/品牌/门店
	StallType domain.StallType `json:"stall_type,omitempty"`
	// 打印类型：收据/小票或标签
	PrintType domain.StallPrintType `json:"print_type,omitempty"`
	// 使用状态，默认启用
	Enabled bool `json:"enabled,omitempty"`
	// 排序，值越小越靠前
	SortOrder int `json:"sort_order,omitempty"`
	// 品牌商ID
	MerchantID uuid.UUID `json:"merchant_id,omitempty"`
	// 门店ID，可为空表示品牌维度
	StoreID uuid.UUID `json:"store_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StallQuery when eager-loading is set.
	Edges        StallEdges `json:"edges"`
	selectValues sql.SelectValues
}

// StallEdges holds the relations/edges for other nodes in the graph.
type StallEdges struct {
	// Merchant holds the value of the merchant edge.
	Merchant *Merchant `json:"merchant,omitempty"`
	// Store holds the value of the store edge.
	Store *Store `json:"store,omitempty"`
	// Devices holds the value of the devices edge.
	Devices []*Device `json:"devices,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// MerchantOrErr returns the Merchant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StallEdges) MerchantOrErr() (*Merchant, error) {
	if e.Merchant != nil {
		return e.Merchant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: merchant.Label}
	}
	return nil, &NotLoadedError{edge: "merchant"}
}

// StoreOrErr returns the Store value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StallEdges) StoreOrErr() (*Store, error) {
	if e.Store != nil {
		return e.Store, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: store.Label}
	}
	return nil, &NotLoadedError{edge: "store"}
}

// DevicesOrErr returns the Devices value or an error if the edge
// was not loaded in eager-loading.
func (e StallEdges) DevicesOrErr() ([]*Device, error) {
	if e.loadedTypes[2] {
		return e.Devices, nil
	}
	return nil, &NotLoadedError{edge: "devices"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Stall) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case stall.FieldEnabled:
			values[i] = new(sql.NullBool)
		case stall.FieldDeletedAt, stall.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case stall.FieldName, stall.FieldStallType, stall.FieldPrintType:
			values[i] = new(sql.NullString)
		case stall.FieldCreatedAt, stall.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case stall.FieldID, stall.FieldMerchantID, stall.FieldStoreID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Stall fields.
func (s *Stall) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case stall.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				s.ID = *value
			}
		case stall.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				s.CreatedAt = value.Time
			}
		case stall.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				s.UpdatedAt = value.Time
			}
		case stall.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				s.DeletedAt = value.Int64
			}
		case stall.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				s.Name = value.String
			}
		case stall.FieldStallType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field stall_type", values[i])
			} else if value.Valid {
				s.StallType = domain.StallType(value.String)
			}
		case stall.FieldPrintType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field print_type", values[i])
			} else if value.Valid {
				s.PrintType = domain.StallPrintType(value.String)
			}
		case stall.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				s.Enabled = value.Bool
			}
		case stall.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				s.SortOrder = int(value.Int64)
			}
		case stall.FieldMerchantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field merchant_id", values[i])
			} else if value != nil {
				s.MerchantID = *value
			}
		case stall.FieldStoreID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value != nil {
				s.StoreID = *value
			}
		default:
			s.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Stall.
// This includes values selected through modifiers, order, etc.
func (s *Stall) Value(name string) (ent.Value, error) {
	return s.selectValues.Get(name)
}

// QueryMerchant queries the "merchant" edge of the Stall entity.
func (s *Stall) QueryMerchant() *MerchantQuery {
	return NewStallClient(s.config).QueryMerchant(s)
}

// QueryStore queries the "store" edge of the Stall entity.
func (s *Stall) QueryStore() *StoreQuery {
	return NewStallClient(s.config).QueryStore(s)
}

// QueryDevices queries the "devices" edge of the Stall entity.
func (s *Stall) QueryDevices() *DeviceQuery {
	return NewStallClient(s.config).QueryDevices(s)
}

// Update returns a builder for updating this Stall.
// Note that you need to call Stall.Unwrap() before calling this method if this Stall
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Stall) Update() *StallUpdateOne {
	return NewStallClient(s.config).UpdateOne(s)
}

// Unwrap unwraps the Stall entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (s *Stall) Unwrap() *Stall {
	_tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Stall is not a transactional entity")
	}
	s.config.driver = _tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Stall) String() string {
	var builder strings.Builder
	builder.WriteString("Stall(")
	builder.WriteString(fmt.Sprintf("id=%v, ", s.ID))
	builder.WriteString("created_at=")
	builder.WriteString(s.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(s.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", s.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(s.Name)
	builder.WriteString(", ")
	builder.WriteString("stall_type=")
	builder.WriteString(fmt.Sprintf("%v", s.StallType))
	builder.WriteString(", ")
	builder.WriteString("print_type=")
	builder.WriteString(fmt.Sprintf("%v", s.PrintType))
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", s.Enabled))
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", s.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("merchant_id=")
	builder.WriteString(fmt.Sprintf("%v", s.MerchantID))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", s.StoreID))
	builder.WriteByte(')')
	return builder.String()
}

// Stalls is a parsable slice of Stall.
type Stalls []*Stall
