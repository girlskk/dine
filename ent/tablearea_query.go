// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.jiguang.dev/pos-dine/dine/ent/dinetable"
	"gitlab.jiguang.dev/pos-dine/dine/ent/predicate"
	"gitlab.jiguang.dev/pos-dine/dine/ent/tablearea"
)

// TableAreaQuery is the builder for querying TableArea entities.
type TableAreaQuery struct {
	config
	ctx            *QueryContext
	order          []tablearea.OrderOption
	inters         []Interceptor
	predicates     []predicate.TableArea
	withDinetables *DineTableQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TableAreaQuery builder.
func (taq *TableAreaQuery) Where(ps ...predicate.TableArea) *TableAreaQuery {
	taq.predicates = append(taq.predicates, ps...)
	return taq
}

// Limit the number of records to be returned by this query.
func (taq *TableAreaQuery) Limit(limit int) *TableAreaQuery {
	taq.ctx.Limit = &limit
	return taq
}

// Offset to start from.
func (taq *TableAreaQuery) Offset(offset int) *TableAreaQuery {
	taq.ctx.Offset = &offset
	return taq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (taq *TableAreaQuery) Unique(unique bool) *TableAreaQuery {
	taq.ctx.Unique = &unique
	return taq
}

// Order specifies how the records should be ordered.
func (taq *TableAreaQuery) Order(o ...tablearea.OrderOption) *TableAreaQuery {
	taq.order = append(taq.order, o...)
	return taq
}

// QueryDinetables chains the current query on the "dinetables" edge.
func (taq *TableAreaQuery) QueryDinetables() *DineTableQuery {
	query := (&DineTableClient{config: taq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := taq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := taq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tablearea.Table, tablearea.FieldID, selector),
			sqlgraph.To(dinetable.Table, dinetable.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, tablearea.DinetablesTable, tablearea.DinetablesColumn),
		)
		fromU = sqlgraph.SetNeighbors(taq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TableArea entity from the query.
// Returns a *NotFoundError when no TableArea was found.
func (taq *TableAreaQuery) First(ctx context.Context) (*TableArea, error) {
	nodes, err := taq.Limit(1).All(setContextOp(ctx, taq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tablearea.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (taq *TableAreaQuery) FirstX(ctx context.Context) *TableArea {
	node, err := taq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TableArea ID from the query.
// Returns a *NotFoundError when no TableArea ID was found.
func (taq *TableAreaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = taq.Limit(1).IDs(setContextOp(ctx, taq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tablearea.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (taq *TableAreaQuery) FirstIDX(ctx context.Context) int {
	id, err := taq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TableArea entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TableArea entity is found.
// Returns a *NotFoundError when no TableArea entities are found.
func (taq *TableAreaQuery) Only(ctx context.Context) (*TableArea, error) {
	nodes, err := taq.Limit(2).All(setContextOp(ctx, taq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tablearea.Label}
	default:
		return nil, &NotSingularError{tablearea.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (taq *TableAreaQuery) OnlyX(ctx context.Context) *TableArea {
	node, err := taq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TableArea ID in the query.
// Returns a *NotSingularError when more than one TableArea ID is found.
// Returns a *NotFoundError when no entities are found.
func (taq *TableAreaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = taq.Limit(2).IDs(setContextOp(ctx, taq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tablearea.Label}
	default:
		err = &NotSingularError{tablearea.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (taq *TableAreaQuery) OnlyIDX(ctx context.Context) int {
	id, err := taq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TableAreas.
func (taq *TableAreaQuery) All(ctx context.Context) ([]*TableArea, error) {
	ctx = setContextOp(ctx, taq.ctx, ent.OpQueryAll)
	if err := taq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TableArea, *TableAreaQuery]()
	return withInterceptors[[]*TableArea](ctx, taq, qr, taq.inters)
}

// AllX is like All, but panics if an error occurs.
func (taq *TableAreaQuery) AllX(ctx context.Context) []*TableArea {
	nodes, err := taq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TableArea IDs.
func (taq *TableAreaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if taq.ctx.Unique == nil && taq.path != nil {
		taq.Unique(true)
	}
	ctx = setContextOp(ctx, taq.ctx, ent.OpQueryIDs)
	if err = taq.Select(tablearea.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (taq *TableAreaQuery) IDsX(ctx context.Context) []int {
	ids, err := taq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (taq *TableAreaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, taq.ctx, ent.OpQueryCount)
	if err := taq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, taq, querierCount[*TableAreaQuery](), taq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (taq *TableAreaQuery) CountX(ctx context.Context) int {
	count, err := taq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (taq *TableAreaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, taq.ctx, ent.OpQueryExist)
	switch _, err := taq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (taq *TableAreaQuery) ExistX(ctx context.Context) bool {
	exist, err := taq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TableAreaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (taq *TableAreaQuery) Clone() *TableAreaQuery {
	if taq == nil {
		return nil
	}
	return &TableAreaQuery{
		config:         taq.config,
		ctx:            taq.ctx.Clone(),
		order:          append([]tablearea.OrderOption{}, taq.order...),
		inters:         append([]Interceptor{}, taq.inters...),
		predicates:     append([]predicate.TableArea{}, taq.predicates...),
		withDinetables: taq.withDinetables.Clone(),
		// clone intermediate query.
		sql:       taq.sql.Clone(),
		path:      taq.path,
		modifiers: append([]func(*sql.Selector){}, taq.modifiers...),
	}
}

// WithDinetables tells the query-builder to eager-load the nodes that are connected to
// the "dinetables" edge. The optional arguments are used to configure the query builder of the edge.
func (taq *TableAreaQuery) WithDinetables(opts ...func(*DineTableQuery)) *TableAreaQuery {
	query := (&DineTableClient{config: taq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	taq.withDinetables = query
	return taq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TableArea.Query().
//		GroupBy(tablearea.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (taq *TableAreaQuery) GroupBy(field string, fields ...string) *TableAreaGroupBy {
	taq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TableAreaGroupBy{build: taq}
	grbuild.flds = &taq.ctx.Fields
	grbuild.label = tablearea.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TableArea.Query().
//		Select(tablearea.FieldCreatedAt).
//		Scan(ctx, &v)
func (taq *TableAreaQuery) Select(fields ...string) *TableAreaSelect {
	taq.ctx.Fields = append(taq.ctx.Fields, fields...)
	sbuild := &TableAreaSelect{TableAreaQuery: taq}
	sbuild.label = tablearea.Label
	sbuild.flds, sbuild.scan = &taq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TableAreaSelect configured with the given aggregations.
func (taq *TableAreaQuery) Aggregate(fns ...AggregateFunc) *TableAreaSelect {
	return taq.Select().Aggregate(fns...)
}

func (taq *TableAreaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range taq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, taq); err != nil {
				return err
			}
		}
	}
	for _, f := range taq.ctx.Fields {
		if !tablearea.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if taq.path != nil {
		prev, err := taq.path(ctx)
		if err != nil {
			return err
		}
		taq.sql = prev
	}
	return nil
}

func (taq *TableAreaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TableArea, error) {
	var (
		nodes       = []*TableArea{}
		_spec       = taq.querySpec()
		loadedTypes = [1]bool{
			taq.withDinetables != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TableArea).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TableArea{config: taq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(taq.modifiers) > 0 {
		_spec.Modifiers = taq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, taq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := taq.withDinetables; query != nil {
		if err := taq.loadDinetables(ctx, query, nodes,
			func(n *TableArea) { n.Edges.Dinetables = []*DineTable{} },
			func(n *TableArea, e *DineTable) { n.Edges.Dinetables = append(n.Edges.Dinetables, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (taq *TableAreaQuery) loadDinetables(ctx context.Context, query *DineTableQuery, nodes []*TableArea, init func(*TableArea), assign func(*TableArea, *DineTable)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*TableArea)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(dinetable.FieldAreaID)
	}
	query.Where(predicate.DineTable(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(tablearea.DinetablesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AreaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "area_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (taq *TableAreaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := taq.querySpec()
	if len(taq.modifiers) > 0 {
		_spec.Modifiers = taq.modifiers
	}
	_spec.Node.Columns = taq.ctx.Fields
	if len(taq.ctx.Fields) > 0 {
		_spec.Unique = taq.ctx.Unique != nil && *taq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, taq.driver, _spec)
}

func (taq *TableAreaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tablearea.Table, tablearea.Columns, sqlgraph.NewFieldSpec(tablearea.FieldID, field.TypeInt))
	_spec.From = taq.sql
	if unique := taq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if taq.path != nil {
		_spec.Unique = true
	}
	if fields := taq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tablearea.FieldID)
		for i := range fields {
			if fields[i] != tablearea.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := taq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := taq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := taq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := taq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (taq *TableAreaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(taq.driver.Dialect())
	t1 := builder.Table(tablearea.Table)
	columns := taq.ctx.Fields
	if len(columns) == 0 {
		columns = tablearea.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if taq.sql != nil {
		selector = taq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if taq.ctx.Unique != nil && *taq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range taq.modifiers {
		m(selector)
	}
	for _, p := range taq.predicates {
		p(selector)
	}
	for _, p := range taq.order {
		p(selector)
	}
	if offset := taq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := taq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (taq *TableAreaQuery) ForUpdate(opts ...sql.LockOption) *TableAreaQuery {
	if taq.driver.Dialect() == dialect.Postgres {
		taq.Unique(false)
	}
	taq.modifiers = append(taq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return taq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (taq *TableAreaQuery) ForShare(opts ...sql.LockOption) *TableAreaQuery {
	if taq.driver.Dialect() == dialect.Postgres {
		taq.Unique(false)
	}
	taq.modifiers = append(taq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return taq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (taq *TableAreaQuery) Modify(modifiers ...func(s *sql.Selector)) *TableAreaSelect {
	taq.modifiers = append(taq.modifiers, modifiers...)
	return taq.Select()
}

// TableAreaGroupBy is the group-by builder for TableArea entities.
type TableAreaGroupBy struct {
	selector
	build *TableAreaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tagb *TableAreaGroupBy) Aggregate(fns ...AggregateFunc) *TableAreaGroupBy {
	tagb.fns = append(tagb.fns, fns...)
	return tagb
}

// Scan applies the selector query and scans the result into the given value.
func (tagb *TableAreaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tagb.build.ctx, ent.OpQueryGroupBy)
	if err := tagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TableAreaQuery, *TableAreaGroupBy](ctx, tagb.build, tagb, tagb.build.inters, v)
}

func (tagb *TableAreaGroupBy) sqlScan(ctx context.Context, root *TableAreaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tagb.fns))
	for _, fn := range tagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tagb.flds)+len(tagb.fns))
		for _, f := range *tagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TableAreaSelect is the builder for selecting fields of TableArea entities.
type TableAreaSelect struct {
	*TableAreaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tas *TableAreaSelect) Aggregate(fns ...AggregateFunc) *TableAreaSelect {
	tas.fns = append(tas.fns, fns...)
	return tas
}

// Scan applies the selector query and scans the result into the given value.
func (tas *TableAreaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tas.ctx, ent.OpQuerySelect)
	if err := tas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TableAreaQuery, *TableAreaSelect](ctx, tas.TableAreaQuery, tas, tas.inters, v)
}

func (tas *TableAreaSelect) sqlScan(ctx context.Context, root *TableAreaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tas.fns))
	for _, fn := range tas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tas *TableAreaSelect) Modify(modifiers ...func(s *sql.Selector)) *TableAreaSelect {
	tas.modifiers = append(tas.modifiers, modifiers...)
	return tas
}
