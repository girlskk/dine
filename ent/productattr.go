// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"gitlab.jiguang.dev/pos-dine/dine/domain"
	"gitlab.jiguang.dev/pos-dine/dine/ent/productattr"
)

// ProductAttr is the model entity for the ProductAttr schema.
type ProductAttr struct {
	config `json:"-"`
	// ID of the ent.
	// UUID as primary key
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// 删除时间
	DeletedAt int64 `json:"deleted_at,omitempty"`
	// 口味做法名称
	Name string `json:"name,omitempty"`
	// 售卖渠道列表
	Channels []domain.SaleChannel `json:"channels,omitempty"`
	// 品牌商ID
	MerchantID uuid.UUID `json:"merchant_id,omitempty"`
	// 门店ID
	StoreID uuid.UUID `json:"store_id,omitempty"`
	// 关联的商品数量（所有项的关联商品数量累加）
	ProductCount int `json:"product_count,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProductAttrQuery when eager-loading is set.
	Edges        ProductAttrEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ProductAttrEdges holds the relations/edges for other nodes in the graph.
type ProductAttrEdges struct {
	// 口味做法项列表
	Items []*ProductAttrItem `json:"items,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ItemsOrErr returns the Items value or an error if the edge
// was not loaded in eager-loading.
func (e ProductAttrEdges) ItemsOrErr() ([]*ProductAttrItem, error) {
	if e.loadedTypes[0] {
		return e.Items, nil
	}
	return nil, &NotLoadedError{edge: "items"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductAttr) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case productattr.FieldChannels:
			values[i] = new([]byte)
		case productattr.FieldDeletedAt, productattr.FieldProductCount:
			values[i] = new(sql.NullInt64)
		case productattr.FieldName:
			values[i] = new(sql.NullString)
		case productattr.FieldCreatedAt, productattr.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case productattr.FieldID, productattr.FieldMerchantID, productattr.FieldStoreID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductAttr fields.
func (pa *ProductAttr) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productattr.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pa.ID = *value
			}
		case productattr.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pa.CreatedAt = value.Time
			}
		case productattr.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pa.UpdatedAt = value.Time
			}
		case productattr.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				pa.DeletedAt = value.Int64
			}
		case productattr.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pa.Name = value.String
			}
		case productattr.FieldChannels:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field channels", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &pa.Channels); err != nil {
					return fmt.Errorf("unmarshal field channels: %w", err)
				}
			}
		case productattr.FieldMerchantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field merchant_id", values[i])
			} else if value != nil {
				pa.MerchantID = *value
			}
		case productattr.FieldStoreID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field store_id", values[i])
			} else if value != nil {
				pa.StoreID = *value
			}
		case productattr.FieldProductCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_count", values[i])
			} else if value.Valid {
				pa.ProductCount = int(value.Int64)
			}
		default:
			pa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductAttr.
// This includes values selected through modifiers, order, etc.
func (pa *ProductAttr) Value(name string) (ent.Value, error) {
	return pa.selectValues.Get(name)
}

// QueryItems queries the "items" edge of the ProductAttr entity.
func (pa *ProductAttr) QueryItems() *ProductAttrItemQuery {
	return NewProductAttrClient(pa.config).QueryItems(pa)
}

// Update returns a builder for updating this ProductAttr.
// Note that you need to call ProductAttr.Unwrap() before calling this method if this ProductAttr
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *ProductAttr) Update() *ProductAttrUpdateOne {
	return NewProductAttrClient(pa.config).UpdateOne(pa)
}

// Unwrap unwraps the ProductAttr entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *ProductAttr) Unwrap() *ProductAttr {
	_tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductAttr is not a transactional entity")
	}
	pa.config.driver = _tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *ProductAttr) String() string {
	var builder strings.Builder
	builder.WriteString("ProductAttr(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pa.ID))
	builder.WriteString("created_at=")
	builder.WriteString(pa.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pa.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(fmt.Sprintf("%v", pa.DeletedAt))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(pa.Name)
	builder.WriteString(", ")
	builder.WriteString("channels=")
	builder.WriteString(fmt.Sprintf("%v", pa.Channels))
	builder.WriteString(", ")
	builder.WriteString("merchant_id=")
	builder.WriteString(fmt.Sprintf("%v", pa.MerchantID))
	builder.WriteString(", ")
	builder.WriteString("store_id=")
	builder.WriteString(fmt.Sprintf("%v", pa.StoreID))
	builder.WriteString(", ")
	builder.WriteString("product_count=")
	builder.WriteString(fmt.Sprintf("%v", pa.ProductCount))
	builder.WriteByte(')')
	return builder.String()
}

// ProductAttrs is a parsable slice of ProductAttr.
type ProductAttrs []*ProductAttr
